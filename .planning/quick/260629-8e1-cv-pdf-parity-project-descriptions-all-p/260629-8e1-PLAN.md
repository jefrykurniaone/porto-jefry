---
phase: 260629-8e1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/cv/cv-types.ts
  - src/components/cv/cv-styles.ts
  - src/components/cv/CvProjects.tsx
  - src/components/cv/CvDocument.tsx
  - src/components/cv/CvHeader.tsx
autonomous: true
requirements: [CV-DESC, CV-ALL-PROJECTS, CV-GITHUB]

must_haves:
  truths:
    - "Downloaded PDF shows all 14 projects (not just 6)"
    - "Each project card shows its localized description text"
    - "Each project card shows its full tech list (no +N truncation chip)"
    - "The id PDF shows Indonesian descriptions and 'Sekarang' for ongoing periods"
    - "The CV header shows github.com/jefrykurniaone after LinkedIn, linking to the GitHub URL"
  artifacts:
    - path: "src/components/cv/cv-types.ts"
      provides: "Messages.projects.items type for per-project descriptions"
      contains: "items: Record<string, { description: string }>"
    - path: "src/components/cv/cv-styles.ts"
      provides: "projectDesc style + GITHUB_URL/GITHUB_DISPLAY re-exports; MAX_PROJECTS/MAX_TECH_TAGS removed"
      contains: "projectDesc"
    - path: "src/components/cv/CvProjects.tsx"
      provides: "All projects with descriptions, full tech, localized period"
      contains: "messages.projects.items"
    - path: "src/components/cv/CvHeader.tsx"
      provides: "GitHub contact link"
      contains: "GITHUB_URL"
  key_links:
    - from: "src/components/cv/CvProjects.tsx"
      to: "messages.projects.items[proj.id].description"
      via: "description resolution in the projects map"
      pattern: "messages\\.projects\\.items"
    - from: "src/components/cv/CvProjects.tsx"
      to: "translatePeriod"
      via: "localized period (mirrors CvExperience)"
      pattern: "translatePeriod"
    - from: "src/components/cv/CvDocument.tsx"
      to: "CvProjects locale prop"
      via: "<CvProjects ... locale={locale} />"
      pattern: "CvProjects.*locale"
    - from: "src/components/cv/CvHeader.tsx"
      to: "GITHUB_URL / GITHUB_DISPLAY"
      via: "import from ./cv-styles"
      pattern: "GITHUB_URL"
---

<objective>
Make the downloaded PDF CV (`/api/generate-cv`) match the website in three ways: render each project's localized description, show ALL 14 projects with their FULL tech lists (remove the 6-project and 4-tag caps), and add GitHub to the header after LinkedIn.

Purpose: The PDF is the artifact recruiters download — it must not silently drop content (descriptions, 8 of 14 projects, GitHub) that the website shows. The compact 2-column card grid stays (user-confirmed); react-pdf auto-paginates to ~2-3 A4 pages.

Output: Five edited files under `src/components/cv/`. No new dependencies, no data changes (descriptions already exist in `src/i18n/messages/{en,id}.json` under `projects.items.<id>.description`; GitHub constants already exist in `src/data/contact.ts`).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md

# Files being edited
@src/components/cv/cv-types.ts
@src/components/cv/cv-styles.ts
@src/components/cv/CvProjects.tsx
@src/components/cv/CvDocument.tsx
@src/components/cv/CvHeader.tsx

# Reuse patterns (read, do not edit)
@src/components/cv/CvExperience.tsx
@src/components/cv/CvCertifications.tsx
@src/utils/translate-period.ts
@src/data/contact.ts
@src/data/projects.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add CV types and styles foundation (additive)</name>
  <files>src/components/cv/cv-types.ts, src/components/cv/cv-styles.ts</files>
  <action>
Additive-only changes so the build stays green; the constant removals happen in Task 2 alongside their only consumer.

In `cv-types.ts`: extend the `projects` field of the `Messages` interface to add an `items` key alongside the existing `title` and `present`. Type it as `Record<string, { description: string }>` — this mirrors the runtime shape the route already passes in from `en.json`/`id.json` and matches the `experience.items` precedent on the same interface. (CV-DESC)

In `cv-styles.ts`: add a `projectDesc` entry to the `styles` StyleSheet, placed in the Projects style group next to `projectName`/`projectMeta`. Model it on the existing `certDesc` style (fontSize 8.5, color DARK, lineHeight 1.4) and add `marginBottom: 3` so the description sits cleanly between the meta line and the tech row. (CV-DESC)

In `cv-styles.ts`: in the existing contact re-export block (the `export { ... } from '@/data/contact'` group), add two aliased re-exports: `CONTACT_GITHUB_URL as GITHUB_URL` and `CONTACT_GITHUB_DISPLAY as GITHUB_DISPLAY`, mirroring the existing `CONTACT_LINKEDIN_URL as LINKEDIN_URL` / `CONTACT_LINKEDIN_DISPLAY as LINKEDIN_DISPLAY` lines. (CV-GITHUB)

Do NOT remove `MAX_PROJECTS` / `MAX_TECH_TAGS` in this task — `CvProjects.tsx` still imports them until Task 2.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>tsc passes; `Messages.projects` has an `items: Record<string, { description: string }>` member; `cv-styles.ts` exports a `projectDesc` style and re-exports `GITHUB_URL` and `GITHUB_DISPLAY`.</done>
</task>

<task type="auto">
  <name>Task 2: Render all projects with descriptions, full tech, and localized periods</name>
  <files>src/components/cv/CvProjects.tsx, src/components/cv/CvDocument.tsx, src/components/cv/cv-styles.ts</files>
  <action>
Rewrite `CvProjects.tsx` to reach parity with the website (`src/components/sections/Projects.tsx`), keeping the existing 2-column `projectRow`/`projectCard` grid.

1. All projects: in the default `CvProjects` component, chunk the FULL `projects` array into rows of 2 — remove the `projects.slice(0, MAX_PROJECTS)` call and the `recentProjects` local; iterate `projects` directly. (CV-ALL-PROJECTS)

2. Descriptions: in the parent map that builds rows, resolve each project's description via `messages.projects.items[proj.id]?.description` (the same lookup shape as `resolveProjectDescription` at `Projects.tsx:50-62`) and pass it into `ProjectCard` as a new `description?: string` prop. In `ProjectCard`, render `<Text style={styles.projectDesc}>{description}</Text>` only when the description is a non-empty string, positioned between the `projectMeta` line and the `tagRow`. (CV-DESC)

3. All tech: in `ProjectCard`, remove the `visibleTech`/`hiddenCount` locals and the `+{hiddenCount}` chip; map over the full `proj.tech` array directly (matches the website `TechList`). (CV-ALL-PROJECTS)

4. Localized period: thread a `locale: string` prop through `CvProjects` and `ProjectCard`, exactly as `CvExperience`/`ExperienceItem` do. In `ProjectCard`, compute the displayed period as `translatePeriod(proj.period.replace('Present', messages.projects.present), locale)` — import `translatePeriod` from `@/utils/translate-period`. The card needs `messages.projects.present` and `locale`; thread whatever is cleanest (pass `present` and `locale` props, or pass `messages` plus `locale`) without leaking unrelated message keys. Render the localized period where `proj.period` currently appears in `projectMeta`.

5. Cleanup: update the import from `./cv-styles` to drop `MAX_PROJECTS` and `MAX_TECH_TAGS` (keep `styles`); remove any other now-unused imports.

In `cv-styles.ts`: delete the `MAX_PROJECTS` and `MAX_TECH_TAGS` constant declarations (verified earlier: `CvProjects.tsx` was their only consumer, now removed). (CV-ALL-PROJECTS)

In `CvDocument.tsx`: pass `locale` to the projects section — change `<CvProjects messages={messages} />` to `<CvProjects messages={messages} locale={locale} />`, mirroring the existing `<CvExperience messages={messages} locale={locale} />` call.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>tsc passes (no lingering references to the removed constants); `CvProjects.tsx` maps the full `projects` array, renders `messages.projects.items[proj.id]?.description` conditionally, renders all `proj.tech`, and uses `translatePeriod(...)` for the period; `CvDocument.tsx` passes `locale` to `CvProjects`; `MAX_PROJECTS`/`MAX_TECH_TAGS` no longer exist anywhere in `src/components/cv/`.</done>
</task>

<task type="auto">
  <name>Task 3: Add GitHub to the CV header and run the full check suite</name>
  <files>src/components/cv/CvHeader.tsx</files>
  <action>
In `CvHeader.tsx`, add GitHub to the contact row after the LinkedIn link, mirroring the LinkedIn block exactly. (CV-GITHUB)

Import `GITHUB_URL` and `GITHUB_DISPLAY` from `./cv-styles` (add them to the existing destructured import that already pulls `LINKEDIN_URL`, `LINKEDIN_DISPLAY`). After the closing `</Link>` of the LinkedIn link, add a pipe separator `<Text style={styles.contactItem}>|</Text>` followed by `<Link style={styles.contactLink} src={GITHUB_URL}>{GITHUB_DISPLAY}</Link>`, matching the existing separator/link pattern used between phone and LinkedIn. No type change is needed — the header uses constants, not translations. `contactRow` already has `flexWrap: 'wrap'`, so the extra item wraps cleanly.

After the edit, run the full automated suite (see verify). This is the plan's final gate.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint && npm run test</automated>
  </verify>
  <done>tsc, lint, and the full test suite all pass; `CvHeader.tsx` renders a GitHub `<Link>` using `GITHUB_URL`/`GITHUB_DISPLAY`, separated from the LinkedIn link by a `|`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /api/generate-cv | `locale` query param crosses here — ALREADY validated against the en/id whitelist in the route (unchanged by this plan) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-cvpar-01 | Injection / Tampering | Project descriptions rendered in PDF | accept | Description text comes from version-controlled `src/i18n/messages/{en,id}.json` (trusted source, no user input); `@react-pdf/renderer` `<Text>` is a PDF primitive that does not interpret HTML/markup, so there is no XSS/injection vector |
| T-cvpar-02 | Information disclosure | GitHub link in header | accept | URL is a hardcoded compile-time constant (`CONTACT_GITHUB_URL`) pointing to an already-public profile; no secret or new data exposed |
| T-cvpar-SC | Tampering | npm/pip/cargo installs | n/a | No package installs in this plan — all dependencies (`@react-pdf/renderer`, `sharp`) already present; no new attack surface |
</threat_model>

<verification>
Automated (run by the executor; aggregated in Task 3):
- `npx tsc --noEmit` — confirms the new `Messages.projects.items` type resolves and no unused-symbol/dangling-import errors remain after removing `MAX_PROJECTS`/`MAX_TECH_TAGS`.
- `npm run lint` — ESLint clean (watch for unused imports in `CvProjects.tsx`).
- `npm run test` — full Vitest suite stays green (CV components are excluded from coverage and no existing test references these CV files — verified via grep).

Manual PDF check (human, REQUIRED before considering the change done):
<human-check>
  1. Restart the dev server (`npm run dev`) — REQUIRED. The route memoizes the rendered PDF per-locale in a module-level `CV_BUFFER_CACHE` and sets `Cache-Control: max-age=3600`, so without a restart a stale PDF is served and changes will not appear.
  2. Open both locales and inspect the PDFs:
     - http://localhost:3000/api/generate-cv?locale=en
     - http://localhost:3000/api/generate-cv?locale=id
  3. Confirm in the EN PDF: all 14 projects present; each card shows its English description and its full tech list (no `+N` chip); header shows `github.com/jefrykurniaone` after LinkedIn (separated by `|`) and the link opens the GitHub profile.
  4. Confirm in the ID PDF: descriptions render in Indonesian; ongoing periods show "Sekarang" (not "Present"); same GitHub link present.
</human-check>
</verification>

<success_criteria>
- All 14 projects render in the PDF (cap removed) in the compact 2-column grid.
- Each project shows its localized description (from `projects.items.<id>.description`) and its complete tech list (no `+N` truncation).
- Project periods are localized for `id` (e.g., "Present" → "Sekarang", month abbreviations translated via `translatePeriod`).
- The header shows GitHub (`github.com/jefrykurniaone`) after LinkedIn, separated by `|`, linking to `CONTACT_GITHUB_URL`.
- `npx tsc --noEmit`, `npm run lint`, and `npm run test` all pass.
- Manual PDF check passes for both `en` and `id` after a dev-server restart.
</success_criteria>

<output>
Create `.planning/quick/260629-8e1-cv-pdf-parity-project-descriptions-all-p/260629-8e1-SUMMARY.md` when done.
</output>
