---
phase: 04-code-quality-type-safety-deferred-phase-7
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/api/generate-cv/route.ts
  - src/app/[locale]/error.tsx
  - src/components/sections/Projects.tsx
  - src/components/sections/About.tsx
autonomous: true
requirements: [QUAL-02]
must_haves:
  truths:
    - "GET in route.ts, Error in error.tsx, Projects in Projects.tsx, and About in About.tsx are each <= 40 lines"
    - "No behaviour, markup, or response shape changes in any of the four files"
    - "These four files produce zero max-lines-per-function violations under the 40-line rule"
  artifacts:
    - path: "src/app/api/generate-cv/route.ts"
      provides: "CV PDF GET handler refactored under the 40-line function limit"
    - path: "src/app/[locale]/error.tsx"
      provides: "Error boundary refactored under the 40-line function limit"
    - path: "src/components/sections/Projects.tsx"
      provides: "Projects section refactored under the 40-line function limit"
    - path: "src/components/sections/About.tsx"
      provides: "About section refactored under the 40-line function limit"
  key_links:
    - from: "refactored sub-components/helpers"
      to: "their parent functions in the same file"
      via: "in-file extraction (private named functions/sub-components)"
      pattern: "function "
---

<objective>
Fix 4 of the 8 remaining max-lines-per-function (40) violations found in a live lint inventory,
so the working tree is already clean when Plan 06 activates the rule at error level (QUAL-02 /
D-04). This plan covers the four simpler files: route.ts (GET = 59 lines), error.tsx
(Error = 43), Projects.tsx (Projects = 49), About.tsx (About = 45). Each fix extracts a cohesive
block into a private named helper or sub-component in the SAME file (following the documented
"private named sub-components within the same file" pattern), with no change to behaviour,
markup, response shape, or rendered DOM.

Purpose: Pre-clean source so CI never goes red the instant the error-level lint gate turns on.
Output: Four refactored files, each with every function body at or under 40 lines.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-code-quality-type-safety-deferred-phase-7/04-CONTEXT.md
@.github/copilot-instructions.md
@src/app/api/generate-cv/route.ts
@src/app/[locale]/error.tsx
@src/components/sections/Projects.tsx
@src/components/sections/About.tsx
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Split GET in route.ts and Error in error.tsx under 40 lines (QUAL-02)</name>
  <read_first>
    - src/app/api/generate-cv/route.ts (GET at line 96, 59 lines: locale resolution, rate-limit guard, cache hit, render+cache+catch)
    - src/app/[locale]/error.tsx (Error at line 12, 43 lines: useEffect log + JSX with two action buttons)
    - .github/copilot-instructions.md (private named sub-components within the same file; no empty catch; max 40 lines/function)
    - .planning/phases/04-code-quality-type-safety-deferred-phase-7/04-CONTEXT.md (D-04: fix violations in-phase so build stays green)
  </read_first>
  <action>
    In src/app/api/generate-cv/route.ts: extract cohesive blocks out of GET into module-scope
    helpers so GET becomes a thin orchestrator at or under 40 lines. Suggested extractions (keep
    names descriptive): resolveLocale(rawLocale) returning a SupportedLocale; rateLimitGuard(ip)
    returning either a 429 NextResponse or null; renderCvResponse(locale) containing the try/catch
    render-cache-return block (returning a NextResponse). Preserve the rate-limit semantics, the
    CV_BUFFER_CACHE lookup, the ipAddress(req) with the 127.0.0.1 fallback, all status codes
    (200/429/500), headers, and the existing console.error on failure (no empty catch). Do NOT
    change the SEC-03 accepted-risk comment block or the rate-limit constants.

    In src/app/[locale]/error.tsx: extract the two-button action group (tryAgain + returnHome Link)
    into a private named sub-component in the same file (e.g. ErrorActions taking reset and locale
    plus pre-resolved labels or the t function). Keep the useEffect console.error log, the icon,
    heading, and message exactly as-is. Error must end up at or under 40 lines.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - In route.ts, the GET function body is at or under 40 lines (verify visually and via a temporary eslint run with max-lines-per-function set to error,40 reporting zero violations for route.ts)
    - route.ts still returns 200 with buildPdfHeaders on success/cache-hit, 429 with Retry-After when rate-limited, and 500 with console.error on render failure (semantics unchanged)
    - route.ts still contains the SEC-03 accepted-risk comment and the ipAddress fallback to 127.0.0.1
    - In error.tsx, the Error function body is at or under 40 lines
    - error.tsx still renders the icon, title, message, a tryAgain button calling reset, and a returnHome Link to /{locale}
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>GET and Error are each at or under 40 lines with identical behaviour; type-check passes.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Split Projects in Projects.tsx and About in About.tsx under 40 lines (QUAL-02)</name>
  <read_first>
    - src/components/sections/Projects.tsx (Projects at line 6, 49 lines: section wrapper + projects.map rendering an sgds-card per project)
    - src/components/sections/About.tsx (About at line 11, 45 lines: section wrapper + sgds-card with a contact-links footer)
    - .github/copilot-instructions.md (private named sub-components within the same file; max 40 lines/function)
    - .planning/phases/04-code-quality-type-safety-deferred-phase-7/04-CONTEXT.md (D-04: fix violations in-phase)
  </read_first>
  <action>
    In src/components/sections/Projects.tsx: extract the per-project card JSX (the contents of the
    projects.map callback) into a private named sub-component in the same file (e.g. ProjectCard
    taking the project item plus the resolved present label and locale, or the t function). Keep
    the section wrapper, heading, grid classes, the external-link icon logic, the
    translatePeriod(...replace('Present', t('present')), locale) call, and the TechList usage
    exactly as-is so rendered DOM is identical. Projects must end up at or under 40 lines.

    In src/components/sections/About.tsx: extract the contact-links footer block (the three a tags
    for email/phone/linkedin inside slot='footer') into a private named sub-component in the same
    file (e.g. AboutContactLinks taking the t function or pre-resolved aria labels). Keep all
    hrefs, aria-labels, classNames, icons, and the description span unchanged. About must end up at
    or under 40 lines.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - In Projects.tsx, the Projects function body is at or under 40 lines and a private ProjectCard-style sub-component renders each project
    - Projects.tsx still maps over projects and renders name, company, translated period, optional description, optional external link, and TechList for each (rendered DOM unchanged)
    - In About.tsx, the About function body is at or under 40 lines and a private sub-component renders the contact links
    - About.tsx still renders the description plus the three contact links (mailto email, phone, linkedin) with their original aria-labels and icons
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>Projects and About are each at or under 40 lines with identical rendered output; type-check passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> /api/generate-cv | Untrusted query param (locale) and request IP cross here; already validated/whitelisted today |
| static section components -> rendered DOM | No untrusted input; portfolio data and i18n are repo-controlled |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-07 | Tampering | route.ts GET refactor (locale + rate-limit handling) | mitigate | Preserve the existing locale whitelist (resolveLocale only returns en/id) and the rate-limit guard semantics during extraction; refactor is structural only, no validation removed. |
| T-04-08 | Spoofing | rate-limit identity in route.ts | accept | The ipAddress(req) fallback to 127.0.0.1 and SEC-01 trusted-IP behaviour are preserved unchanged by the refactor; no new spoofing surface. |
| T-04-09 | Information Disclosure | error.tsx refactor | accept | The error boundary continues to log to console only and never renders stack traces to users; extracting the button group changes no error-surface behaviour. |

No new high-severity threats are introduced. All four refactors are structural extractions that
preserve existing validation, status codes, and error handling.
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0
- Temporary eslint probe with rules max-lines-per-function set to [error,40] reports zero violations for route.ts, error.tsx, Projects.tsx, About.tsx
- No diff in rendered DOM / response shape (markup and headers/status codes preserved)
</verification>

<success_criteria>
- QUAL-02 (partial): GET, Error, Projects, and About are each at or under 40 lines.
- No behaviour, markup, status code, or response shape changes in any of the four files.
- These four files contribute zero violations to the gate Plan 06 activates.
</success_criteria>

<output>
Create `.planning/phases/04-code-quality-type-safety-deferred-phase-7/04-04-SUMMARY.md` when done.
</output>
