---
phase: 04-code-quality-type-safety-deferred-phase-7
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/projects.ts
autonomous: true
requirements: [QUAL-03]
must_haves:
  truths:
    - "Each exact-duplicate tech array in projects.ts is replaced by a single named TECH_* constant reference"
    - "No tech string array literal is duplicated verbatim across project entries"
    - "src/data/projects.ts stays under 300 lines"
    - "Every project still renders the identical tech list it did before (no value changes)"
  artifacts:
    - path: "src/data/projects.ts"
      provides: "projects array with shared tech stacks extracted as TECH_* constants"
      contains: "TECH_"
  key_links:
    - from: "project entries in src/data/projects.ts"
      to: "TECH_* constants at top of src/data/projects.ts"
      via: "tech: TECH_* reference"
      pattern: "tech: TECH_"
---

<objective>
Extract the verbatim-duplicate `tech` arrays in `src/data/projects.ts` into named
`SCREAMING_SNAKE_CASE` `TECH_*` constants (QUAL-03). A live inventory found exactly two
exact-duplicate arrays: the Sitefinity/.NET stack (appears in 4 projects) and the
VB.NET/ASP.NET Web Forms stack (appears in 4 projects). Per D-05 extract ONLY these verbatim
duplicates; per D-05a leave near-duplicates (e.g. the Sitecore variant and the C#/ASP.NET MVC
variants with differing trailing entries) inline. Per D-05b the file is 232 lines so the
constants live at the top of `projects.ts` (a separate `src/data/tech-stacks.ts` is only
required if extraction would push the file over 300 lines, which it will not).

Purpose: Eliminate duplicated string arrays so a stack edit happens in one place, and keep the
file under the 300-line limit Plan 05 enforces.
Output: An updated `src/data/projects.ts` with two `TECH_*` constants and no duplicated tech array.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-code-quality-type-safety-deferred-phase-7/04-CONTEXT.md
@.github/copilot-instructions.md
@src/data/projects.ts
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Extract the two verbatim-duplicate tech arrays into TECH_* constants (QUAL-03)</name>
  <read_first>
    - src/data/projects.ts (full file: ProjectItem interface + projects array; 232 lines)
    - .planning/phases/04-code-quality-type-safety-deferred-phase-7/04-CONTEXT.md (D-05 verbatim duplicates only; D-05a near-duplicates stay inline; D-05b constants at top of projects.ts unless it would exceed 300 lines)
    - .github/copilot-instructions.md (SCREAMING_SNAKE_CASE for constants; data-module pattern exports interface + typed array)
  </read_first>
  <action>
    Declare two module-scope `readonly`-friendly string-array constants at the top of
    `src/data/projects.ts` (after the `ProjectItem` interface, before the `projects` array),
    using descriptive SCREAMING_SNAKE_CASE names prefixed `TECH_`:
    (a) `TECH_SITEFINITY_DOTNET` = `['Sitefinity', 'C#', '.NET Framework MVC', 'HTML', 'CSS', 'JavaScript', 'SQL Server']`
        — the exact array currently duplicated in the 4 projects:
        yellow-ribbon-singapore-website, public-service-commission-website,
        agency-for-science-technology-and-research, defense-science-and-technology-agency.
    (b) `TECH_VBNET_WEBFORMS` = `['VB.NET', 'ASP.NET Web Forms', 'SSRS', 'SSIS', 'HTML', 'CSS', 'JavaScript', 'SQL Server', 'GIT']`
        — the exact array currently duplicated in the 4 projects:
        rbbr-super-bank, oprisk-super-bank, orms-adira-insurance, orms-btpn.
    Type each constant so it remains assignable to `ProjectItem['tech']` (string[]); if you use
    `as const`, spread it (`tech: [...TECH_SITEFINITY_DOTNET]`) to preserve a mutable `string[]`
    shape, otherwise type the constant as `string[]` and reference it directly. Replace each of
    the 8 matching inline `tech: [...]` blocks with a reference to the matching constant. Do NOT
    touch the heritagesg Sitecore array (near-duplicate: `Sitecore` not `Sitefinity`) or any of
    the C#/ASP.NET MVC arrays (near-duplicates with differing trailing entries) — those stay
    inline per D-05a. The set of tech strings rendered for every project MUST be byte-identical
    to before (order preserved).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `src/data/projects.ts` contains `TECH_SITEFINITY_DOTNET` and `TECH_VBNET_WEBFORMS` declared at module scope (descriptive SCREAMING_SNAKE_CASE, prefixed `TECH_`)
    - The literal array `'Sitefinity', 'C#', '.NET Framework MVC', 'HTML', 'CSS', 'JavaScript', 'SQL Server'` appears at most ONCE in the file (only in the constant declaration)
    - The literal array `'VB.NET', 'ASP.NET Web Forms', 'SSRS', 'SSIS'` appears at most ONCE in the file (only in the constant declaration)
    - Exactly 8 project entries reference a `TECH_*` constant for their `tech` field (4 Sitefinity + 4 VB.NET)
    - The heritagesg Sitecore array and the C#/ASP.NET MVC arrays remain inline (unchanged)
    - `src/data/projects.ts` is under 300 lines (`wc -l` < 300)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Both verbatim-duplicate stacks are single TECH_* constants; no duplicated tech array remains; file under 300 lines; type-check passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| static data module → section components | Version-controlled portfolio data compiled into the bundle; not user-supplied |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-06 | Tampering | extracted TECH_* constants in projects.ts | accept | Pure refactor of static repo-controlled data; constants hold the identical string values previously inlined. No runtime data-source change, no user input. |

No `high`-severity threats are introduced. This is a static-data deduplication refactor with
no behaviour, dependency, or attack-surface change.
</threat_model>

<verification>
- `grep -c "TECH_SITEFINITY_DOTNET" src/data/projects.ts` returns at least 5 (1 declaration + 4 references)
- `grep -c "TECH_VBNET_WEBFORMS" src/data/projects.ts` returns at least 5 (1 declaration + 4 references)
- `wc -l src/data/projects.ts` is under 300
- `npx tsc --noEmit` exits 0
- Rendered tech lists unchanged (values preserved verbatim, order preserved)
</verification>

<success_criteria>
- QUAL-03: Shared tech stacks extracted as named `TECH_*` constants — no string arrays duplicated across project entries.
- `src/data/projects.ts` stays under 300 lines.
- Every project renders the identical tech list as before.
</success_criteria>

<output>
Create `.planning/phases/04-code-quality-type-safety-deferred-phase-7/04-03-SUMMARY.md` when done.
</output>
