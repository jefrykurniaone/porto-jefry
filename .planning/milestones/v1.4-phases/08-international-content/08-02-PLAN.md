---
phase: 08-international-content
plan: 02
type: execute
wave: 1
depends_on: [08-01]
files_modified:
  - src/i18n/messages/en.json
  - src/i18n/messages/id.json
  - src/components/sections/Projects.tsx
  - src/data/education.ts
  - src/components/sections/Projects.test.tsx
autonomous: false
requirements: [CONTENT-02, CONTENT-03]
must_haves:
  truths:
    - "All 14 projects have bilingual descriptions sourced from i18n (projects.items.<id>.description)"
    - "Projects.tsx reads descriptions via next-intl with a missing-key guard"
    - "Education majors read 'Diploma in Informatics Management' and 'Bachelor's in Information Systems'"
    - "en.json and id.json stay key-for-key parallel (translations.test.ts passes)"
  artifacts:
    - path: "src/components/sections/Projects.tsx"
      provides: "Project cards reading bilingual descriptions from i18n"
      contains: "items."
    - path: "src/data/education.ts"
      provides: "Idiomatic English education majors"
---

<objective>
Add bilingual project descriptions (CONTENT-02) and fix non-idiomatic education terms
(CONTENT-03). The 14 projects currently render no description; route descriptions through i18n
so they are bilingual, and update `Projects.tsx` to read them. Fix the two education majors to
the owner-supplied idiomatic English.

Purpose: Give each project a short, sector-aware description for international readers, and make
education read correctly to an overseas audience.
Output: `projects.items.<id>.description` for all 14 projects in both locales, a `Projects.tsx`
that reads them safely, and corrected `education.ts` majors/degrees.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-international-content/08-CONTEXT.md
@src/data/projects.ts
@src/components/sections/Projects.tsx
@src/data/education.ts
@src/i18n/messages/en.json
@src/i18n/messages/id.json
@src/i18n/messages/translations.test.ts
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Draft bilingual project descriptions in i18n (CONTENT-02)</name>
  <read_first>
    - src/data/projects.ts (14 projects: id, name, company, period, tech[]; sectors inferable — gov/Sitefinity for Xtremax, banking/insurance ORMS/OPRISK/RBBR for Nawa, HR/event for Arkamaya)
    - src/i18n/messages/en.json (existing `projects` namespace: `title`, `present`)
    - 08-CONTEXT.md (D-08-06, D-08-07: i18n-based, drafted, flagged for owner verification)
  </read_first>
  <action>
    Add a `projects.items.<id>.description` string for each of the 14 project ids in `en.json`,
    using the humanizer voice — one concise, concrete sentence per project drawn from its name,
    company, sector, and tech. Keep claims truthful and generic enough to be safe (these are
    real client projects the owner will verify). Then mirror all 14 in `id.json` idiomatically.
    Keep key parity exact.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `projects.items.<id>.description` exists for all 14 ids in BOTH `en.json` and `id.json`
    - Descriptions are concise, concrete, and non-buzzword
  </acceptance_criteria>
  <done>14 bilingual project descriptions drafted in i18n.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Read descriptions from i18n in Projects.tsx (CONTENT-02)</name>
  <read_first>
    - src/components/sections/Projects.tsx (`ProjectCard` renders `project.description` ~lines 37-41; `useTranslations('projects')` available; iterates by `project.id`)
    - next-intl docs: `t.has(...)` / safe lookup to avoid throwing on a missing key
  </read_first>
  <action>
    Update `Projects.tsx` to read the description from i18n via
    `t('items.' + project.id + '.description')` instead of `project.description`, guarded so a
    project without a description does not throw (use `t.has('items.' + id + '.description')` or
    an equivalent safe check, rendering the paragraph only when present). Pass the translator
    down to `ProjectCard` (or look it up there). Leave the `ProjectItem.description` field in
    `projects.ts` unused/removed as appropriate (no data descriptions are populated).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run test -- src/components/sections/Projects.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `Projects.tsx` renders descriptions from `t('items.<id>.description')` with a missing-key guard
    - Project cards show the new descriptions; no runtime throw for any project
    - `Projects.test.tsx` updated and passing
  </acceptance_criteria>
  <done>Projects render bilingual descriptions from i18n.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Fix education majors to idiomatic English (CONTENT-03)</name>
  <read_first>
    - src/data/education.ts (`major` fields: Telkom Diploma "Informatic Management"; Widyatama Bachelor's "System Information")
    - 08-CONTEXT.md (D-08-08: owner-supplied mapping)
  </read_first>
  <action>
    Update `education.ts`: Telkom University Diploma major → "Informatics Management" (degree
    label "Diploma in Informatics Management" where shown); Widyatama Bachelor's major →
    "Information Systems" (degree label "Bachelor's in Information Systems"). Adjust any
    co-located education test/snapshot that asserts the old strings.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run test -- src/data 2>/dev/null || npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `education.ts` majors read "Informatics Management" and "Information Systems"
    - Any education test/snapshot updated to match
  </acceptance_criteria>
  <done>Education majors are idiomatic English.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Parity + full CI (CONTENT-02, CONTENT-03)</name>
  <action>Run the key-parity test and full CI gate.</action>
  <verify>
    <automated>npm run lint && npx tsc --noEmit && npm run test && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `translations.test.ts` passes (all 14 description keys parallel in both locales)
    - lint, typecheck, test, build all exit 0; coverage thresholds hold
  </acceptance_criteria>
  <done>Project descriptions + education fixes shipped; full CI green.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-02-01 | N/A | i18n description lookup | accept | Keys are static repo-controlled strings; the `t.has` guard prevents a missing-key throw. No user input, no new surface. |

No high-severity threats.
</threat_model>

<verification>
- All 14 `projects.items.<id>.description` present in en.json and id.json; project cards render them
- `Projects.tsx` guards missing keys (no throw)
- Education majors read the idiomatic English strings
- `npm run lint && npx tsc --noEmit && npm run test && npm run build` exit 0
</verification>

<success_criteria>
- CONTENT-02: 14 bilingual project descriptions from i18n; `Projects.tsx` reads via next-intl with a guard.
- CONTENT-03: Education majors/degrees use idiomatic English.
</success_criteria>

<output>
Create `.planning/phases/08-international-content/08-02-SUMMARY.md` when done.
</output>
