---
phase: 08-international-content
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/i18n/messages/en.json
  - src/i18n/messages/id.json
autonomous: false
requirements: [CONTENT-01]
must_haves:
  truths:
    - "A humanizer skill is installed (via find-skills) and used to drive the rewrite voice"
    - "hero.subtitle/title, about.description, all experience bullets, contact.description, certifications.coding_id.description are rewritten in a natural non-buzzword voice"
    - "About/Contact copy includes a clear remote/international availability signal"
    - "No fabricated metrics — only [your number] placeholders where a real figure would help"
    - "en.json and id.json stay key-for-key parallel (translations.test.ts passes)"
  artifacts:
    - path: "src/i18n/messages/en.json"
      provides: "Humanized English prose with availability signal"
    - path: "src/i18n/messages/id.json"
      provides: "Parallel idiomatic Indonesian prose"
---

<objective>
Rewrite the core portfolio prose (CONTENT-01) in a natural, specific, human voice for
international employers, replacing the current buzzword-heavy resume-speak. Install a humanizer
skill first and use it to drive the voice. Add a remote/international availability signal. Do
not invent metrics.

Purpose: Make the writing read like a real person and appeal to overseas hiring managers.
Output: Rewritten `en.json` strings (hero, about, experience bullets, contact, certification)
mirrored idiomatically in `id.json`, with key parity preserved.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-international-content/08-CONTEXT.md
@src/i18n/messages/en.json
@src/i18n/messages/id.json
@src/i18n/messages/translations.test.ts
</context>

<tasks>

<task type="checkpoint" tdd="false">
  <name>Task 1: Install a humanizer skill (CONTENT-01 setup)</name>
  <read_first>
    - 08-CONTEXT.md (D-08-01: humanizer-first)
  </read_first>
  <action>
    Use the `find-skills` skill to locate and install a humanizer skill (content humanizer /
    natural-writing style). If exactly one good match exists, install it. If multiple plausible
    matches exist, surface the options to the owner (checkpoint) and let them choose before
    proceeding. Confirm the skill is installed/available before any rewrite.
  </action>
  <verify>
    <manual>The chosen humanizer skill appears in the available skills list and can be invoked.</manual>
  </verify>
  <acceptance_criteria>
    - A humanizer skill is installed and confirmed available (owner-approved if multiple candidates)
  </acceptance_criteria>
  <done>Humanizer skill ready to drive the rewrite.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Rewrite hero, about, contact, certification prose in en.json (CONTENT-01)</name>
  <read_first>
    - src/i18n/messages/en.json (`hero.subtitle`, `hero.title`, `about.description`, `contact.description`, `certifications.coding_id.description`)
    - 08-CONTEXT.md (D-08-02..05: plain concrete first-person; availability signal; no fabricated metrics)
  </read_first>
  <action>
    Using the humanizer skill, rewrite `hero.subtitle` (and tighten `hero.title` if it helps),
    `about.description`, `contact.description`, and `certifications.coding_id.description` into a
    natural, specific voice. Keep all facts truthful (5+ years .NET/C#, banking/insurance/HR/gov
    sectors, current role at PT Xtremax, React/Next.js + Docker + Semantic Kernel/OpenAI
    exploration). Add a clear, natural remote/international availability signal in
    `about.description` and/or `contact.description`. Do NOT invent numbers; if a metric would
    strengthen a line, leave a literal `[your number]` placeholder. Preserve every JSON key name.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <manual>The rewritten EN copy reads naturally, has no buzzword filler, and includes an availability signal.</manual>
  </verify>
  <acceptance_criteria>
    - `hero.subtitle`, `about.description`, `contact.description`, `certifications.coding_id.description` rewritten; key names unchanged
    - An availability signal is present in about and/or contact
    - No invented metrics; any metric is a `[your number]` placeholder
  </acceptance_criteria>
  <done>Hero/about/contact/cert prose humanized in EN.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Rewrite all experience bullets in en.json (CONTENT-01)</name>
  <read_first>
    - src/i18n/messages/en.json (`experience.items.<id>.bullets[]` for: xtremax-2025, arkamaya-2024, nawa-lead-2024, nawa-onerisk-2023, xtremax-2022, nawa-2020)
    - 08-CONTEXT.md (nawa-2020 bullets are the model — real clients, concrete impact)
  </read_first>
  <action>
    Rewrite every role's `bullets` into impact-oriented, concrete statements (what was built /
    owned / improved, for which sector), in the humanized voice. Keep the same array lengths and
    keys unless a bullet genuinely merges/splits (prefer keeping count stable for parity). No
    fabricated metrics — use `[your number]` placeholders where a real figure would help.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Every `experience.items.*.bullets` entry is rewritten in the natural voice
    - No duty-only filler ("seamless integration", "exceptional user experiences") remains
    - No invented metrics
  </acceptance_criteria>
  <done>All experience bullets humanized in EN.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Mirror every change in id.json + parity + CI (CONTENT-01)</name>
  <read_first>
    - src/i18n/messages/id.json (same keys as en.json)
    - src/i18n/messages/translations.test.ts (key-parity guard)
    - 08-CONTEXT.md (D-08-09: idiomatic Indonesian, not literal)
  </read_first>
  <action>
    Apply the same rewrites to `id.json` in idiomatic Indonesian (mirror meaning and availability
    signal, not a word-for-word translation). Keep `[your number]` placeholders identical. Ensure
    every key in en.json has a matching key in id.json and vice versa. Run the parity test and
    full CI.
  </action>
  <verify>
    <automated>npm run lint && npx tsc --noEmit && npm run test && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `id.json` mirrors all rewritten keys idiomatically; key parity holds
    - `translations.test.ts` passes
    - lint, typecheck, test, build all exit 0
  </acceptance_criteria>
  <done>EN/ID prose humanized with parity; full CI green.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01-01 | N/A | Static i18n copy | accept | Repo-controlled translation strings compiled into the bundle; not user-supplied. Content-only change, no new surface. |

No high-severity threats.
</threat_model>

<verification>
- Hero/about/contact/cert/experience copy reads naturally in EN and ID; availability signal present
- No buzzword filler; no fabricated metrics (only `[your number]` placeholders)
- `npm run test` passes including `translations.test.ts` key parity
- `npm run lint && npx tsc --noEmit && npm run test && npm run build` exit 0
</verification>

<success_criteria>
- CONTENT-01: Core prose rewritten in a humanizer-driven natural voice + remote/international availability signal; no fabricated metrics; EN/ID parity preserved.
</success_criteria>

<output>
Create `.planning/phases/08-international-content/08-01-SUMMARY.md` when done.
</output>
