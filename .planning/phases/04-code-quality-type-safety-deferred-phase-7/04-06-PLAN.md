---
phase: 04-code-quality-type-safety-deferred-phase-7
plan: 06
type: execute
wave: 2
depends_on:
  - 04-01
  - 04-02
  - 04-03
  - 04-04
  - 04-05
files_modified:
  - .eslintrc.json
  - package.json
  - package-lock.json
autonomous: true
requirements: [QUAL-02]
must_haves:
  truths:
    - "eslint-plugin-sonarjs is installed and the no-nested-template-literals rule is active at error level"
    - "max-lines (300) and max-lines-per-function (40) are active at error level repo-wide"
    - "Test files (*.test.ts / *.test.tsx) are exempt from the length rules via an overrides block"
    - "npm run lint exits 0 with the new rules active (no source violations remain)"
    - "npm run build exits 0 (lint -> typecheck -> test -> build all pass)"
  artifacts:
    - path: ".eslintrc.json"
      provides: "Active error-level max-lines, max-lines-per-function, sonarjs/no-nested-template-literals with test-file overrides"
      contains: "max-lines-per-function"
    - path: "package.json"
      provides: "eslint-plugin-sonarjs devDependency"
      contains: "eslint-plugin-sonarjs"
  key_links:
    - from: ".eslintrc.json (plugins + rules)"
      to: "eslint-plugin-sonarjs"
      via: "plugins: [sonarjs] + rule sonarjs/no-nested-template-literals"
      pattern: "sonarjs"
    - from: ".eslintrc.json (overrides)"
      to: "*.test.ts / *.test.tsx"
      via: "overrides block turning length rules off for the test glob"
      pattern: "overrides"
---

<objective>
Activate the QUAL-02 lint enforcement now that all source violations are fixed in Wave 1. Install
eslint-plugin-sonarjs and wire three rules at error level in the legacy .eslintrc.json: the core
rules max-lines (300) and max-lines-per-function (40), plus sonarjs/no-nested-template-literals
(D-02). Exempt test files (*.test.ts / *.test.tsx) from the length rules via an overrides block
(D-03). Because Wave 1 already cleaned every source violation and no nested template literals
exist in src/** non-test code (confirmed by inventory), npm run lint and the full CI chain stay
green the instant the rules turn on (D-04). This is the gate plan and MUST run after plans
04-01..04-05.

Purpose: Make the coding-standard limits real and CI-blocking, without ever handing off a red build.
Output: Updated .eslintrc.json (rules + overrides), eslint-plugin-sonarjs added to devDependencies.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-code-quality-type-safety-deferred-phase-7/04-CONTEXT.md
@.github/copilot-instructions.md
@.eslintrc.json
@package.json
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify eslint-plugin-sonarjs legitimacy before install (supply-chain gate)</name>
  <what-built>
    Nothing is installed yet. This is a package-legitimacy checkpoint required before adding a new
    package-manager dependency. Planning research already confirmed: eslint-plugin-sonarjs is
    published by SonarSource (LGPL-3.0-only), latest 4.x, peerDependency eslint ^8.0.0 || ^9 || ^10
    (matches this repo's eslint ^8), ships CJS (cjs/plugin.js) so it works with legacy .eslintrc,
    and exposes the rule no-nested-template-literals (S3358) plus a recommended-legacy config.
  </what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/eslint-plugin-sonarjs and confirm it is published by
       SonarSource, actively maintained, with weekly downloads in the millions.
    2. Confirm the latest 4.x version and that peerDependencies list eslint ^8.0.0 (or a range
       including ^8).
    3. Confirm the license is LGPL-3.0-only and acceptable for this project.
    4. Type approved to proceed with the install, or describe concerns to halt.
  </how-to-verify>
  <read_first>
    - package.json (devDependencies: eslint ^8, eslint-config-next 14.2.35)
    - .planning/phases/04-code-quality-type-safety-deferred-phase-7/04-CONTEXT.md (D-04a: lint policy delegated to Claude; SonarJS plugin chosen for no-nested-template-literals)
  </read_first>
  <acceptance_criteria>
    - Human has confirmed eslint-plugin-sonarjs is the genuine SonarSource package on npmjs.com
    - Human has confirmed eslint ^8 compatibility and an acceptable license
    - Human typed approved (this checkpoint is never auto-approved)
  </acceptance_criteria>
  <resume-signal>Type approved to install eslint-plugin-sonarjs, or describe concerns.</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Install eslint-plugin-sonarjs and activate the three rules at error level (QUAL-02)</name>
  <read_first>
    - .eslintrc.json (currently only extends next/core-web-vitals and next/typescript)
    - package.json (devDependencies; confirm eslint version ^8)
    - .github/copilot-instructions.md (max 300 lines/file, 40 lines/function — the limits these rules codify)
    - .planning/phases/04-code-quality-type-safety-deferred-phase-7/04-CONTEXT.md (D-02 error level; D-03 exempt test files via overrides; D-04 build must stay green; implementation note: max-lines and max-lines-per-function are core rules, no-nested-template-literals is from eslint-plugin-sonarjs; keep legacy .eslintrc.json format)
  </read_first>
  <action>
    Install the plugin as a dev dependency: run npm install --save-dev eslint-plugin-sonarjs
    (this updates package.json and package-lock.json). Then edit .eslintrc.json (keep the legacy
    eslintrc format): add a plugins array containing sonarjs; add a rules block setting
    max-lines to [error, 300], max-lines-per-function to [error, 40], and
    sonarjs/no-nested-template-literals to error. Add an overrides block whose files glob is
    ["**/*.test.ts", "**/*.test.tsx"] (or ["*.test.ts", "*.test.tsx"] matching the existing
    co-located test naming) that turns max-lines and max-lines-per-function off for test files
    (D-03). Keep the existing extends entries (next/core-web-vitals, next/typescript). Do NOT
    enable the full sonarjs recommended set — enable only the single named rule to avoid
    introducing unrelated violations. Reference the rule as sonarjs/no-nested-template-literals.
  </action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - package.json devDependencies contains eslint-plugin-sonarjs
    - .eslintrc.json contains a plugins array including sonarjs
    - .eslintrc.json contains max-lines set to error with limit 300, max-lines-per-function set to error with limit 40, and sonarjs/no-nested-template-literals set to error
    - .eslintrc.json contains an overrides block matching the test-file glob that disables max-lines and max-lines-per-function for *.test.ts / *.test.tsx
    - npm run lint exits 0 (zero errors — Wave 1 cleaned all source violations and no nested template literals exist in src non-test code)
  </acceptance_criteria>
  <done>The three rules are active at error level with test files exempt; npm run lint passes clean.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Full CI-chain verification (lint -> typecheck -> test -> build) (QUAL-02)</name>
  <read_first>
    - package.json (scripts: lint, test, build; CI order lint -> typecheck -> test -> build per CLAUDE.md)
    - .eslintrc.json (the activated rules from Task 2)
    - .planning/phases/04-code-quality-type-safety-deferred-phase-7/04-CONTEXT.md (D-04: no broken CI handed off)
  </read_first>
  <action>
    Run the full CI chain in order to prove the phase keeps the build green end to end with the new
    gate active and all Wave 1 refactors merged: npm run lint, then npx tsc --noEmit, then
    npm run test, then npm run build. All four MUST pass. If any step fails, the failure is a
    regression introduced by rule activation and must be fixed in the offending Wave 1 file before
    this plan is considered done (do not relax the rules to make it pass; do not exempt non-test
    source files). Confirm the prebuild codegen from Plan 02 still runs as part of npm run build and
    that the sitemap lastmod equals the last git commit date.
  </action>
  <verify>
    <automated>npm run lint && npx tsc --noEmit && npm run test && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - npm run lint exits 0
    - npx tsc --noEmit exits 0
    - npm run test exits 0 (coverage thresholds still met; test files exempt from length rules)
    - npm run build exits 0 and the prebuild codegen regenerates LAST_MODIFIED_DATE to the last git commit date
    - No source (non-test) file was exempted from the length rules to achieve a green build
  </acceptance_criteria>
  <done>The full CI chain passes with the QUAL-02 gate active; no red build handed off.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry -> devDependency install | A new package (eslint-plugin-sonarjs) is added to the toolchain; runs only at lint/dev time, not in production bundle |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-SC | Tampering | eslint-plugin-sonarjs npm install | mitigate | Blocking human-verify checkpoint (Task 1) confirms the genuine SonarSource package on npmjs.com, eslint ^8 compatibility, and license before install. Dev-only dependency; not shipped to users. This checkpoint is never auto-approved. |
| T-04-12 | Denial of Service (CI break) | activating error-level rules | mitigate | Rules are activated only in Wave 2 after Wave 1 (plans 04-01..04-05) cleaned every source violation; Task 3 runs the full CI chain to confirm green before completion. |

No high-severity threats are introduced. The only new package is a dev-time lint plugin gated by
a blocking legitimacy checkpoint.
</threat_model>

<verification>
- `npm run lint` exits 0 with the three rules active at error level
- `grep -c "eslint-plugin-sonarjs" package.json` returns at least 1
- `.eslintrc.json` contains max-lines, max-lines-per-function, no-nested-template-literals, and an overrides block for the test glob
- `npx tsc --noEmit` exits 0; `npm run test` exits 0; `npm run build` exits 0
</verification>

<success_criteria>
- QUAL-02: eslint-plugin-sonarjs installed and active; npm run lint fails on max-lines, max-lines-per-function, and no-nested-template-literals violations (rules at error level).
- Test files are exempt from the length rules via overrides.
- The full CI chain (lint -> typecheck -> test -> build) passes green — no broken build handed off.
</success_criteria>

<output>
Create `.planning/phases/04-code-quality-type-safety-deferred-phase-7/04-06-SUMMARY.md` when done.
</output>
