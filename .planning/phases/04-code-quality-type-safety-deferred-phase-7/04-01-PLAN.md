---
phase: 04-code-quality-type-safety-deferred-phase-7
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/sections/Experience.tsx
autonomous: true
requirements: [TYPE-01, TYPE-02]
must_haves:
  truths:
    - "Experience component reads bullet text through useTranslations-typed access, not useMessages() + double cast"
    - "ExperienceMessages is a top-level exported interface importable from Experience.tsx"
    - "Rendered Experience output is byte-identical to before — same bullets keyed by exp.id"
    - "TypeScript strict mode reports zero errors and no `as unknown as` cast remains"
  artifacts:
    - path: "src/components/sections/Experience.tsx"
      provides: "Typed Experience section with exported ExperienceMessages interface"
      contains: "export interface ExperienceMessages"
  key_links:
    - from: "src/components/sections/Experience.tsx"
      to: "src/i18n/messages/en.json (experience.items.<id>.bullets)"
      via: "useTranslations('experience') typed access"
      pattern: "useTranslations\\('experience'\\)"
---

<objective>
Eliminate the type-safety workaround in the Experience section: lift the inline
`ExperienceMessages` interface to a top-level exported interface (TYPE-01) and replace the
`useMessages()` + `messages.experience as unknown as ExperienceMessages` double cast with
idiomatic `useTranslations`-based typed access (TYPE-02). While in this file, also split
`ExperienceCard` (currently 45 lines) so it satisfies the max-lines-per-function (40) limit
that Plan 06 will activate at error level — this keeps CI green when the lint gate turns on.

Purpose: Remove a known type-safety hack so TypeScript strict mode passes without casts and
the i18n access pattern is idiomatic and self-documenting.
Output: A refactored `src/components/sections/Experience.tsx` with the same rendered output,
an exported `ExperienceMessages` interface, no `useMessages` import, and no `as unknown as`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-code-quality-type-safety-deferred-phase-7/04-CONTEXT.md
@.github/copilot-instructions.md
@src/components/sections/Experience.tsx
@src/i18n/messages/en.json
@src/i18n/messages/id.json
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Lift ExperienceMessages to a top-level exported interface (TYPE-01)</name>
  <read_first>
    - src/components/sections/Experience.tsx (interface declared inside `Experience()` body at ~line 66; cast at ~line 69)
    - src/i18n/messages/en.json (shape: `experience.items.<id>.bullets` is `string[]`; item ids: xtremax-2025, arkamaya-2024, nawa-lead-2024, nawa-onerisk-2023, xtremax-2022, nawa-2020)
    - .planning/phases/04-code-quality-type-safety-deferred-phase-7/04-CONTEXT.md (D-06: exported interface, mechanical refactor, preserve `bullets={expItems[exp.id]?.bullets ?? []}` keyed by exp.id)
    - .github/copilot-instructions.md (PascalCase for types; `export interface` data-module pattern)
  </read_first>
  <action>
    Move the `ExperienceMessages` interface out of the `Experience()` function body to module
    scope (top of file, after imports) and prefix it with `export` per D-06. Keep its shape
    exactly `{ items: Record<string, { bullets: string[] }> }` to match the en.json/id.json
    `experience.items.<id>.bullets` structure. Do not change any consumer logic yet — the
    existing `expItems[exp.id]?.bullets ?? []` access keyed by `exp.id` MUST be preserved.
    This task only relocates and exports the interface; the cast removal is Task 2.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/sections/Experience.tsx` contains the literal `export interface ExperienceMessages`
    - The interface is declared at module scope (not inside any function body) — it appears before the line `export default function Experience`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>ExperienceMessages is a top-level exported interface; type-check passes.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Replace useMessages double cast with useTranslations typed access and split ExperienceCard (TYPE-02 + length fix)</name>
  <read_first>
    - src/components/sections/Experience.tsx (current `useMessages()` import on line 3 and `useMessages()` call on line 64; `messages.experience as unknown as ExperienceMessages` on line 69)
    - src/i18n/messages/en.json + id.json (confirm `experience.items` is a plain object map of id → { bullets: string[] })
    - .planning/phases/04-code-quality-type-safety-deferred-phase-7/04-CONTEXT.md (D-06: use `useTranslations`-based access, e.g. `t.raw(...)` typed against the exported interface; `useMessages()` must no longer be called; preserve behaviour exactly)
  </read_first>
  <action>
    Remove `useMessages` from the `next-intl` import on line 3 (keep `useTranslations` and
    `useLocale`). Delete the `const messages = useMessages()` call. Read the items map via the
    already-present `t = useTranslations('experience')` using `t.raw('items')`, and assert it
    against the exported `ExperienceMessages['items']` type (i.e. cast the single `t.raw('items')`
    result to `ExperienceMessages['items']`, NOT through `unknown`). This is the idiomatic
    next-intl mechanism for reading a structured (non-string) message subtree; `t.raw` returns
    the raw message value typed as `any`, so a single direct assertion to the exported interface
    type fully replaces the prior `as unknown as` double cast. Keep `const expItems = ... ?? {}`
    and the per-card access `expItems[exp.id]?.bullets ?? []` unchanged so rendered bullets are
    byte-identical. The file must contain NEITHER `useMessages` NOR `as unknown as` after this task.

    Additionally, split `ExperienceCard` (currently 45 lines, over the 40-line limit Plan 06
    activates) by extracting the bullet-list `<ul>` block into a private named sub-component in
    the same file (e.g. `ExperienceBulletList` taking `bullets: string[]`), following the
    established "private named sub-components within the same file" pattern documented in
    .github/copilot-instructions.md (as used in Navbar.tsx). Do not change markup, classNames,
    or rendered DOM — only relocate the JSX so each function body is ≤ 40 lines.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run src/components/sections/Experience.test.tsx 2>/dev/null || npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/sections/Experience.tsx` does NOT contain the substring `useMessages`
    - `src/components/sections/Experience.tsx` does NOT contain the substring `as unknown as`
    - `src/components/sections/Experience.tsx` contains `useTranslations('experience')`
    - The access `expItems[exp.id]?.bullets ?? []` (keyed by exp.id) is still present
    - No function in the file exceeds 40 lines (verify: temporarily run eslint with `max-lines-per-function: [error,40]` reports zero violations for this file, or visually confirm each `function`/arrow component body ≤ 40 lines)
    - `npx tsc --noEmit` exits 0
    - Any existing Experience test file still passes (`npx vitest run src/components/sections/Experience.test.tsx` exits 0 if the file exists)
  </acceptance_criteria>
  <done>useMessages and the double cast are gone; bullets render identically; every function in the file is ≤ 40 lines; type-check and tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-time i18n message JSON → component | Static, version-controlled translation files compiled into the bundle; not user-supplied |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | i18n message access in Experience.tsx | accept | Messages are static repo-controlled JSON; the `t.raw('items')` assertion only changes typing, not runtime data source. No new attack surface; behaviour is byte-identical to current code. |
| T-04-02 | Information Disclosure | Exported ExperienceMessages interface | accept | A type-only export carries no runtime data or secrets; it describes the existing public message shape. No disclosure risk. |

No `high`-severity threats are introduced. This task is a type-level/structural refactor with
no change to data flow, no new dependency, and no new user-facing surface.
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0 (strict mode, zero errors, no casts)
- `grep -c "useMessages" src/components/sections/Experience.tsx` returns 0
- `grep -c "as unknown as" src/components/sections/Experience.tsx` returns 0
- `grep -c "export interface ExperienceMessages" src/components/sections/Experience.tsx` returns 1
- Rendered Experience output unchanged (manual diff of DOM not required — markup untouched; access keyed by exp.id preserved)
</verification>

<success_criteria>
- TYPE-01: `ExperienceMessages` is a top-level exported interface — no inline interface, no double cast.
- TYPE-02: Experience bullets accessed via `useTranslations` typed access — `useMessages()` no longer called in the component.
- Experience section renders the same bullets keyed by `exp.id` as before (no behaviour change).
- Experience.tsx introduces zero new max-lines-per-function violations (ready for Plan 06's gate).
</success_criteria>

<output>
Create `.planning/phases/04-code-quality-type-safety-deferred-phase-7/04-01-SUMMARY.md` when done.
</output>
