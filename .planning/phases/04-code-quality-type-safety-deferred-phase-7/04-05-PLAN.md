---
phase: 04-code-quality-type-safety-deferred-phase-7
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/layout/Navbar.tsx
  - src/components/sections/Hero.tsx
  - src/components/sections/Contact.tsx
  - src/hooks/use-focus-trap.ts
  - src/hooks/use-cv-download.ts
autonomous: true
requirements: [QUAL-02]
must_haves:
  truths:
    - "Navbar in Navbar.tsx, HeroCtaButtons and Hero in Hero.tsx, and Contact in Contact.tsx are each <= 40 lines"
    - "No behaviour, markup, focus-trap, scroll, download, or rendered DOM changes in any file"
    - "These files produce zero max-lines-per-function violations under the 40-line rule"
  artifacts:
    - path: "src/components/layout/Navbar.tsx"
      provides: "Navbar refactored under the 40-line function limit"
    - path: "src/components/sections/Hero.tsx"
      provides: "Hero + HeroCtaButtons refactored under the 40-line function limit"
    - path: "src/components/sections/Contact.tsx"
      provides: "Contact refactored under the 40-line function limit"
    - path: "src/hooks/use-focus-trap.ts"
      provides: "Extracted mobile-menu focus-trap effect as a reusable hook"
    - path: "src/hooks/use-cv-download.ts"
      provides: "Extracted CV download logic as a reusable hook"
  key_links:
    - from: "src/components/layout/Navbar.tsx"
      to: "src/hooks/use-focus-trap.ts"
      via: "import useFocusTrap"
      pattern: "useFocusTrap"
    - from: "src/components/sections/Hero.tsx"
      to: "src/hooks/use-cv-download.ts"
      via: "import useCvDownload"
      pattern: "useCvDownload"
---

<objective>
Fix the remaining 4 max-lines-per-function (40) violations from the live lint inventory, in the
three heavier files: Navbar.tsx (Navbar = 105 lines), Hero.tsx (HeroCtaButtons = 81, Hero = 43),
and Contact.tsx (Contact = 54). This completes the source pre-clean (QUAL-02 / D-04) so the
working tree is fully green before Plan 06 activates the error-level lint gate. Extractions follow
the established patterns: large effect/logic blocks move into custom hooks under src/hooks/
(kebab-case filenames per the project convention), and JSX blocks move into private named
sub-components. No behaviour, focus-trap, scroll, download, markup, or rendered DOM changes.

Purpose: Eliminate the last function-length violations so CI stays green when the gate turns on.
Output: Three refactored components plus two new custom hooks, all functions at or under 40 lines.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-code-quality-type-safety-deferred-phase-7/04-CONTEXT.md
@.github/copilot-instructions.md
@src/components/layout/Navbar.tsx
@src/components/sections/Hero.tsx
@src/components/sections/Contact.tsx
@src/hooks/useSgdsTheme.ts
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Extract Navbar effects into hooks and split Navbar under 40 lines (QUAL-02)</name>
  <read_first>
    - src/components/layout/Navbar.tsx (Navbar at line 74, 105 lines: scroll-state effect, focus-trap effect, close-focus effect, scrollTo callback, header JSX)
    - src/hooks/useSgdsTheme.ts (existing custom-hook style in this repo to mirror for new hooks)
    - .github/copilot-instructions.md (utility/hook files use kebab-case e.g. use-auth.ts; private named sub-components; max 40 lines/function)
    - .planning/phases/04-code-quality-type-safety-deferred-phase-7/04-CONTEXT.md (D-04: fix violations in-phase so build stays green)
  </read_first>
  <action>
    Reduce the Navbar function body to at or under 40 lines by extracting cohesive blocks. Create
    src/hooks/use-focus-trap.ts exporting useFocusTrap(isOpen, menuRef, toggleRef, onClose) that
    contains the current mobile-menu focus-trap useEffect (focus first element, Escape closes and
    refocuses toggle, Tab/Shift+Tab wrap) verbatim in behaviour. Optionally also extract the
    scroll-state useEffect into a small hook (e.g. src/hooks/use-scrolled.ts returning isScrolled)
    or keep it inline if Navbar already fits under 40 lines after the focus-trap extraction —
    choose the minimal set of extractions that brings Navbar to at or under 40 lines. You may also
    extract the header JSX into a private sub-component in Navbar.tsx if needed. Preserve the
    passive scroll listener, the NAV_KEYS rendering via DesktopNavLinks/MobileNavLinks, the
    scrollTo behaviour (close menu, smooth scroll, pushState hash), all aria attributes, and the
    return-focus-on-close behaviour exactly. Do NOT change DesktopNavLinks or MobileNavLinks. If
    you add src/hooks/use-scrolled.ts, add it to files_modified mentally (it is owned by this plan).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - src/hooks/use-focus-trap.ts exists and exports a useFocusTrap hook containing the mobile-menu focus-trap logic (Escape close, Tab wrap, initial focus)
    - In Navbar.tsx, the Navbar function body is at or under 40 lines (verify visually and via a temporary eslint run with max-lines-per-function error,40 reporting zero violations for Navbar.tsx)
    - Navbar.tsx still registers the scroll listener with { passive: true }, renders DesktopNavLinks + MobileNavLinks, and preserves scrollTo (close + smooth scroll + history.pushState hash)
    - Navbar still returns focus to the toggle button when the menu closes (skipping initial mount)
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>Navbar is at or under 40 lines via extracted hook(s); focus-trap, scroll, and nav behaviour identical; type-check passes.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Extract CV download hook, split HeroCtaButtons + Hero under 40 lines (QUAL-02)</name>
  <read_first>
    - src/components/sections/Hero.tsx (HeroCtaButtons at line 16, 81 lines: isDownloading/errorMessage state, scrollTo helpers, handleDownload async fetch, CTA JSX, error alert; Hero at line 98, 43 lines)
    - src/hooks/useSgdsTheme.ts (existing hook style)
    - .github/copilot-instructions.md (kebab-case hook filenames; private named sub-components; no empty catch; max 40 lines/function)
    - .planning/phases/04-code-quality-type-safety-deferred-phase-7/04-CONTEXT.md (D-04: fix violations in-phase)
  </read_first>
  <action>
    Create src/hooks/use-cv-download.ts exporting useCvDownload(locale, t) that encapsulates the
    isDownloading and errorMessage state plus the handleDownload async logic (fetch
    /api/generate-cv with the locale query, throw on non-ok with the HTTP status, blob -> object
    URL -> anchor click -> revoke, on error set the localized cv_error message, auto-clear after
    5000ms, console.error with context, finally clear isDownloading). It returns
    { isDownloading, errorMessage, handleDownload }. Preserve the keep-the-eslint-disable
    no-console comment behaviour by logging via console.error with context (no empty catch). In
    Hero.tsx, call this hook inside HeroCtaButtons and keep only the CTA JSX and error alert there;
    if HeroCtaButtons is still over 40 lines, extract the scrollTo/scrollToWithHash helpers and/or
    the CTA link group JSX into a private named sub-component in the same file. Reduce Hero (the
    default export) to at or under 40 lines by extracting the photo + heading/subtitle block into a
    private sub-component if needed. Preserve all classNames, sgds-* elements, aria attributes, the
    download filename Jefry_Kurniawan_CV.pdf, and the rendered DOM exactly.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - src/hooks/use-cv-download.ts exists and exports useCvDownload returning isDownloading, errorMessage, and handleDownload with the fetch/blob/anchor/revoke + localized error + 5000ms auto-clear + console.error logic preserved
    - In Hero.tsx, HeroCtaButtons and Hero function bodies are each at or under 40 lines (verify via a temporary eslint run with max-lines-per-function error,40 reporting zero violations for Hero.tsx)
    - Hero.tsx still renders the photo Image, greeting/name/title/subtitle, the three CTAs (View Work, Download CV with loading state, Contact), and the danger alert on download error (rendered DOM unchanged)
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>HeroCtaButtons and Hero are each at or under 40 lines via the extracted hook/sub-component; download and rendering behaviour identical; type-check passes.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Split Contact under 40 lines (QUAL-02)</name>
  <read_first>
    - src/components/sections/Contact.tsx (Contact at line 52, 54 lines: builds a links array then renders a grid of ContactCard; ContactCard sub-component already exists)
    - .github/copilot-instructions.md (private named sub-components / helpers; max 40 lines/function)
    - .planning/phases/04-code-quality-type-safety-deferred-phase-7/04-CONTEXT.md (D-04: fix violations in-phase)
  </read_first>
  <action>
    Reduce the Contact function body to at or under 40 lines by extracting the links array
    construction into a helper (e.g. a module function buildContactLinks(t) returning
    ContactLinkItem[], or a small useContactLinks helper) so Contact only calls the helper and
    renders the grid. Keep all four links (email, phone, linkedin, github) with identical labels,
    hrefs, values, icons, and isExternal flags, and keep the existing ContactCard sub-component and
    grid markup unchanged. Rendered DOM must be identical.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - In Contact.tsx, the Contact function body is at or under 40 lines (verify via a temporary eslint run with max-lines-per-function error,40 reporting zero violations for Contact.tsx)
    - Contact.tsx still renders all four contact cards (email, phone, linkedin, github) with their original labels, hrefs, values, icons, and external-link flags
    - The existing ContactCard sub-component is preserved
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>Contact is at or under 40 lines with identical rendered output; type-check passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> /api/generate-cv (via Hero download) | The Hero CV download issues a fetch with a locale query param; the endpoint already validates it |
| static layout/section components -> rendered DOM | Repo-controlled data and i18n; no untrusted input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-10 | Tampering | use-cv-download.ts refactor | accept | The download hook preserves the existing fetch URL construction (URLSearchParams with locale) and error handling; it adds no new input path and the server still validates the locale. |
| T-04-11 | Information Disclosure | error handling in use-cv-download.ts / use-focus-trap.ts | accept | Errors are logged via console.error with context (no empty catch) and the user-facing message stays the localized cv_error string; no stack traces or internals are exposed. |

No new high-severity threats are introduced. All extractions are structural and preserve
existing validation, fetch construction, and error handling.
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0
- Temporary eslint probe with rules max-lines-per-function set to [error,40] reports zero violations for Navbar.tsx, Hero.tsx, Contact.tsx
- src/hooks/use-focus-trap.ts and src/hooks/use-cv-download.ts exist and are imported by Navbar.tsx and Hero.tsx respectively
- No diff in rendered DOM or download behaviour
</verification>

<success_criteria>
- QUAL-02 (partial): Navbar, HeroCtaButtons, Hero, and Contact are each at or under 40 lines.
- No behaviour, focus-trap, scroll, download, markup, or rendered DOM changes.
- These files contribute zero violations to the gate Plan 06 activates.
</success_criteria>

<output>
Create `.planning/phases/04-code-quality-type-safety-deferred-phase-7/04-05-SUMMARY.md` when done.
</output>
