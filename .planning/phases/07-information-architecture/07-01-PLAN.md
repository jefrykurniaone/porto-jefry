---
phase: 07-information-architecture
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/sections/Contact.tsx
  - src/components/sections/About.tsx
  - src/i18n/messages/en.json
  - src/i18n/messages/id.json
  - src/components/sections/Contact.test.tsx
  - src/components/sections/About.test.tsx
autonomous: true
requirements: [IA-01]
must_haves:
  truths:
    - "The About section renders a GitHub link (icon + handle) using CONTACT_GITHUB_URL"
    - "The Contact section no longer renders a GitHub card; its grid stays balanced (3 cards)"
    - "The GitHub URL is defined only in src/data/contact.ts"
    - "The label key lives under about.* in both en.json and id.json (key parity holds)"
  artifacts:
    - path: "src/components/sections/About.tsx"
      provides: "About section with a GitHub link"
      contains: "CONTACT_GITHUB_URL"
    - path: "src/components/sections/Contact.tsx"
      provides: "Contact section without a GitHub card"
  key_links:
    - from: "src/components/sections/About.tsx"
      to: "src/data/contact.ts (CONTACT_GITHUB_URL)"
      via: "import"
      pattern: "CONTACT_GITHUB_URL"
---

<objective>
Move the GitHub link from the Contact ("Get In Touch") section to the About section (IA-01).
The URL stays defined once in `src/data/contact.ts`; only the rendering location and the i18n
label namespace change.

Purpose: Surface GitHub alongside the other personal links in About; declutter the contact grid.
Output: `Contact.tsx` without the GitHub card (3 balanced cards), `About.tsx` with a new GitHub
link, the label key moved `contact.github_label` → `about.contact_github` in both locales, and
updated tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-information-architecture/07-CONTEXT.md
@src/components/sections/Contact.tsx
@src/components/sections/About.tsx
@src/data/contact.ts
@src/components/icons/GitHubIcon.tsx
@src/i18n/messages/en.json
@src/i18n/messages/id.json
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Remove GitHub from Contact.tsx + rebalance grid (IA-01)</name>
  <read_first>
    - src/components/sections/Contact.tsx (`buildContactLinks(t)` — GitHub is the 4th object ~lines 77-83; `GitHubIcon` + `CONTACT_GITHUB_URL`/`CONTACT_GITHUB_DISPLAY` imports; grid `ContactCard`s use `sgds-col-lg-3`)
    - 07-CONTEXT.md (D-07-02, D-07-03)
  </read_first>
  <action>
    Delete the GitHub object from `buildContactLinks` so only Email/Phone/LinkedIn remain.
    Remove the now-unused `GitHubIcon` import and the `CONTACT_GITHUB_URL`/`CONTACT_GITHUB_DISPLAY`
    imports from `Contact.tsx`. Change the contact card column width from `sgds-col-lg-3` to
    `sgds-col-lg-4` so the remaining 3 cards form a balanced row. Do not touch the other three
    links or the section heading/description.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `Contact.tsx` `buildContactLinks` returns 3 entries (no GitHub)
    - No `GitHubIcon` / `CONTACT_GITHUB_*` imports remain in `Contact.tsx` (no unused-import lint error)
    - Contact cards use `sgds-col-lg-4`
  </acceptance_criteria>
  <done>Contact section shows 3 balanced cards, no GitHub.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add GitHub link to About.tsx + i18n key (IA-01)</name>
  <read_first>
    - src/components/sections/About.tsx (`AboutContactLinks` props + the LinkedIn anchor ~lines 34-42 to mirror; the `t('contact_*')` label wiring)
    - src/data/contact.ts (`CONTACT_GITHUB_URL`, `CONTACT_GITHUB_HANDLE`)
    - src/components/icons/GitHubIcon.tsx (signature, e.g. `size` prop)
    - src/i18n/messages/en.json + id.json (`contact.github_label` to remove; add `about.contact_github`)
    - src/i18n/messages/translations.test.ts (key-parity guard — both files must match)
  </read_first>
  <action>
    In `About.tsx`: import `GitHubIcon` and `CONTACT_GITHUB_URL` + `CONTACT_GITHUB_HANDLE`; add a
    `githubLabel` prop to `AboutContactLinks`; render a 4th `<a>` mirroring the LinkedIn anchor
    (GitHub icon + `CONTACT_GITHUB_HANDLE` text, `target="_blank"`, `rel="noopener noreferrer"`,
    `aria-label={githubLabel}`). Pass `githubLabel={t('contact_github')}` from `About()`.
    In i18n: add `about.contact_github` ("GitHub" / Indonesian equivalent) to BOTH `en.json` and
    `id.json`, and remove `contact.github_label` from both. Grep first to confirm nothing else
    references `contact.github_label`.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run test -- src/i18n/messages/translations.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `About.tsx` renders a GitHub `<a>` using `CONTACT_GITHUB_URL` + `CONTACT_GITHUB_HANDLE`
    - `about.contact_github` exists in both `en.json` and `id.json`; `contact.github_label` removed from both
    - Translation key-parity test passes
  </acceptance_criteria>
  <done>About shows a GitHub link; label key moved with parity intact.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Update tests + full CI (IA-01)</name>
  <read_first>
    - src/components/sections/Contact.test.tsx (any assertion that GitHub renders in Contact — must be removed/inverted)
    - src/components/sections/About.test.tsx (add an assertion that GitHub renders in About)
  </read_first>
  <action>
    Update `Contact.test.tsx` to assert GitHub is NOT in the contact grid (and the 3 remaining
    links are). Update `About.test.tsx` to assert the GitHub link renders with the correct href.
    Run the full CI gate.
  </action>
  <verify>
    <automated>npm run lint && npx tsc --noEmit && npm run test && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `Contact.test.tsx` no longer expects GitHub; `About.test.tsx` expects GitHub
    - lint, typecheck, test, build all exit 0; coverage thresholds hold
  </acceptance_criteria>
  <done>Tests reflect the move; full CI green.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01-01 | N/A | Static link relocation | accept | Same repo-controlled URL, same `rel="noopener noreferrer"` on the external anchor. No new surface. |

No high-severity threats.
</threat_model>

<verification>
- About renders a GitHub link to `CONTACT_GITHUB_URL`; Contact does not
- `grep -rc "contact.github_label" src` returns 0; `about.contact_github` present in en.json and id.json
- `npm run lint && npx tsc --noEmit && npm run test && npm run build` exit 0
</verification>

<success_criteria>
- IA-01: GitHub link rendered in About, removed from Contact; URL defined once in `data/contact.ts`; label under `about.*` with EN/ID parity; tests updated.
</success_criteria>

<output>
Create `.planning/phases/07-information-architecture/07-01-SUMMARY.md` when done.
</output>
