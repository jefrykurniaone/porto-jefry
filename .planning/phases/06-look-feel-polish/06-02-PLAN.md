---
phase: 06-look-feel-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/globals.css
  - src/components/sections/Hero.tsx
  - src/components/sections/Hero.test.tsx
autonomous: true
requirements: [UI-06]
must_haves:
  truths:
    - "The three hero CTAs render at equal height and equal width"
    - "'View My Work' never wraps to a second line at any desktop width"
    - "Below 512px the three CTAs stack full-width (existing behavior preserved)"
    - "Visual hierarchy is 1 filled primary (View My Work) + 2 outline (Download CV, Contact Me)"
  artifacts:
    - path: "src/app/globals.css"
      provides: "Unified, no-wrap hero CTA sizing"
      contains: "white-space: nowrap"
    - path: "src/components/sections/Hero.tsx"
      provides: "Balanced three-CTA hero with consistent variants"
---

<objective>
Rebalance the three hero CTA buttons (UI-06). Today they are built from two different
element types — two `<a>` anchors (`.hero-cta-link`) and one `<sgds-button>` (CV download) —
sized by two different CSS mechanisms, so heights (48px vs 44px) and widths (148px fixed vs
168px min) mismatch and the longest label ("View My Work") wraps to two lines.

Purpose: A balanced, single-line, equal-sized CTA row that reads as one filled primary action
plus two outline secondary actions.
Output: Updated `globals.css` (unified sizing + `white-space: nowrap`) and `Hero.tsx`
(consistent outline variants), with `Hero.test.tsx` updated.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-look-feel-polish/06-CONTEXT.md
@src/app/globals.css
@src/components/sections/Hero.tsx
@src/components/sections/Hero.test.tsx
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Unify CTA sizing + prevent wrap in globals.css (UI-06)</name>
  <read_first>
    - src/app/globals.css (`.hero-cta-group`, `.hero-cta-link` with `width: 9.25rem; height: 3rem`, `.hero-cta-link-primary`, `.hero-cta-link-outline`, `.hero-download-button sgds-button::part(button)` with `height: 2.75rem; min-width: 10.5rem`, and the `@media (max-width: 512px)` full-width block)
    - 06-CONTEXT.md (D-06-05, D-06-06)
  </read_first>
  <action>
    In `.hero-cta-link`: add `white-space: nowrap`; replace `width: 9.25rem` with
    `min-width: 11.5rem` and `width: auto` (keep `height: 3rem` / `min-height: 3rem`). In
    `.hero-download-button sgds-button::part(button)`: change `height`/`min-height` to `3rem`
    and `min-width` to `11.5rem` so the CV button matches the anchors exactly. Leave the
    `@media (max-width: 512px)` rules (which force `width: 100%` on all three) untouched so
    mobile still stacks full-width. The shared `min-width` guarantees a common floor while
    `width: auto` + `nowrap` lets every label fit on one line.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <manual>At desktop widths the three buttons are the same height and width and "View My Work" is on one line; below 512px they stack full-width.</manual>
  </verify>
  <acceptance_criteria>
    - `.hero-cta-link` contains `white-space: nowrap` and no longer has a fixed `width: 9.25rem`
    - `.hero-download-button ... ::part(button)` height is `3rem` and `min-width` matches `.hero-cta-link`
    - The `@media (max-width: 512px)` full-width rules are unchanged
  </acceptance_criteria>
  <done>Three CTAs share height/width, never wrap on desktop, stack on mobile.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Consistent variants in Hero.tsx + tests (UI-06)</name>
  <read_first>
    - src/components/sections/Hero.tsx (`HeroCtaLinks`: View My Work anchor uses `hero-cta-link-primary`; Contact anchor uses `hero-cta-link-outline`; CV is `<sgds-button>`)
    - src/components/sections/Hero.test.tsx (existing CTA assertions)
    - 06-CONTEXT.md (D-06-07: View My Work = filled primary; Download CV + Contact = outline; CV stays sgds-button for the loading state)
  </read_first>
  <action>
    Make the visual hierarchy "1 primary + 2 outline": keep View My Work as
    `hero-cta-link-primary`; ensure Contact Me stays `hero-cta-link-outline`; set the CV
    `<sgds-button>` to an outline variant (e.g. `variant="outline"`) so it reads as a secondary
    action and matches the outline anchors. Do not change labels, the download handler, the
    loading branch, or scroll handlers. Update `Hero.test.tsx` so its CTA assertions match the
    new variant/markup (still asserting all three labels render and are actionable).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run test -- src/components/sections/Hero.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - View My Work renders filled-primary; Download CV and Contact Me render outline
    - All three labels still render; the CV button keeps its `loading`/`onDownload` behavior
    - `Hero.test.tsx` passes
  </acceptance_criteria>
  <done>Hero shows a balanced 1-primary + 2-outline CTA row; tests green.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Full CI</name>
  <action>Run the complete CI gate to confirm nothing regressed.</action>
  <verify>
    <automated>npm run lint && npx tsc --noEmit && npm run test && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - lint, typecheck, test, build all exit 0; coverage thresholds hold
  </acceptance_criteria>
  <done>Full CI green.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-02-01 | N/A | Hero CTA markup/CSS | accept | Presentational only; same handlers, same href targets, no new input or surface. |

No high-severity threats.
</threat_model>

<verification>
- "View My Work" renders on one line at >=768px and >=1024px
- Three CTAs share computed height and width on desktop; stack full-width <512px
- `npm run lint && npx tsc --noEmit && npm run test && npm run build` exit 0
</verification>

<success_criteria>
- UI-06: Hero shows three equal-sized CTAs; "View My Work" never wraps; full-width stack below 512px; 1 filled primary + 2 outline.
</success_criteria>

<output>
Create `.planning/phases/06-look-feel-polish/06-02-SUMMARY.md` when done.
</output>
