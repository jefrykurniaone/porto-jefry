---
phase: 06-look-feel-polish
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/layout/ThemeToggle.client.tsx
  - src/components/layout/LanguageToggle.tsx
  - src/components/layout/Navbar.tsx
  - src/app/globals.css
  - src/components/layout/ThemeToggle.test.tsx
  - src/components/layout/LanguageToggle.test.tsx
autonomous: true
requirements: [UI-07]
must_haves:
  truths:
    - "Theme toggle is a sliding sgds-switch (checked = night) with sun/moon affordance"
    - "Language toggle is a compact segmented EN|ID pill with a sliding active indicator"
    - "useSgdsTheme contract and router.replace locale logic are unchanged"
    - "Both controls remain in the navbar slot=end and are keyboard-accessible with aria-labels"
  artifacts:
    - path: "src/components/layout/ThemeToggle.client.tsx"
      provides: "sgds-switch based theme toggle"
      contains: "sgds-switch"
    - path: "src/components/layout/LanguageToggle.tsx"
      provides: "segmented EN|ID pill language toggle"
---

<objective>
Redesign the two navbar controls (UI-07). The owner finds the current 44x44 `sgds-icon-button`
(theme) and `sgds-button` EN/ID (language) oversized. Replace them with a sun/moon sliding
`<sgds-switch>` for theme and a compact segmented **EN|ID pill** for language. `sgds-switch`
is already registered globally via `src/app/sgds.tsx`, so no new dependency.

Purpose: Smaller, modern toggle controls that keep all existing behavior and accessibility.
Output: Rebuilt `ThemeToggle.client.tsx` and `LanguageToggle.tsx`, supporting CSS in
`globals.css`, tightened `Navbar.tsx` spacing, and updated tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-look-feel-polish/06-CONTEXT.md
@src/components/layout/ThemeToggle.client.tsx
@src/components/layout/LanguageToggle.tsx
@src/components/layout/Navbar.tsx
@src/hooks/useSgdsTheme.ts
@src/app/sgds.tsx
@src/app/globals.css
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Theme toggle as a sliding sgds-switch (UI-07)</name>
  <read_first>
    - src/components/layout/ThemeToggle.client.tsx (current `sgds-icon-button` sun/moon, forced 44x44, `useSgdsTheme`, `isMounted` guard, `theme.toggle_*` labels)
    - src/hooks/useSgdsTheme.ts (returns `{ theme, toggleTheme }`; theme is `'day' | 'night'`; do NOT change this contract)
    - src/app/sgds.tsx (confirms all SGDS components — including `sgds-switch` — are registered via one dynamic import)
    - src/types/sgds.d.ts (SGDS React type augmentation — `sgds-switch` typing)
  </read_first>
  <action>
    Replace the `sgds-icon-button` with an `<sgds-switch>` whose `checked` reflects
    `theme === 'night'`, and whose change event calls `toggleTheme`. Flank it with small
    sun/moon `sgds-icon`s (or use the switch's label slot) so the direction reads sun→moon.
    Keep the `isMounted`/`suppressHydrationWarning` pattern used by sibling SGDS components to
    avoid hydration mismatch. Keep the aria-label sourced from `theme.toggle_dark`/`toggle_light`.
    Do not modify `useSgdsTheme`. Verify the SGDS switch change event name from the React
    typings (`onSlChange`/`onChange`) and wire accordingly.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <manual>Toggling the switch flips between light and dark and persists across reload (localStorage `sgds-theme`).</manual>
  </verify>
  <acceptance_criteria>
    - `ThemeToggle.client.tsx` renders `<sgds-switch>` with `checked` bound to `theme === 'night'`
    - Change handler calls `toggleTheme`; `useSgdsTheme` is unchanged
    - An accessible label from `theme.toggle_*` is present; no hydration warning
  </acceptance_criteria>
  <done>Theme toggle is a working sun/moon sliding switch.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Language toggle as a segmented EN|ID pill (UI-07)</name>
  <read_first>
    - src/components/layout/LanguageToggle.tsx (current `sgds-button` showing EN/ID; `useRouter`/`usePathname` from `@/i18n/routing`; `router.replace(pathname, { locale })`; `lang.switch` aria-label)
    - 06-CONTEXT.md (D-06-10: segmented pill with sliding indicator; radio-group semantics; keep routing logic)
    - src/app/globals.css (where to add the pill styles, near the other layout/component rules)
  </read_first>
  <action>
    Rebuild `LanguageToggle` as a compact two-segment pill (EN | ID). Use an accessible
    control — either a radiogroup (`role="group"` with two `aria-pressed` buttons, or native
    radios visually styled) — with a sliding active-indicator behind the active segment. The
    active segment reflects the current `useLocale()`; clicking the inactive segment calls the
    existing `router.replace(pathname, { locale: nextLocale })`. Keep the `lang.switch`
    aria-label on the group. Add the pill CSS to `globals.css` (rounded container, active
    indicator that slides between the two halves, monochrome palette). Keep keyboard
    operability (Tab to focus, Enter/Space to activate; arrow keys if using radios).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <manual>The pill shows the active locale highlighted; clicking the other half navigates and the indicator slides; keyboard works.</manual>
  </verify>
  <acceptance_criteria>
    - `LanguageToggle.tsx` renders a two-segment EN|ID pill (not a single text button)
    - The active segment matches `useLocale()`; switching calls `router.replace(pathname, { locale })`
    - Group carries the `lang.switch` aria-label; control is keyboard-accessible
  </acceptance_criteria>
  <done>Language toggle is a working segmented EN|ID pill with a sliding indicator.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Tighten navbar spacing + update tests + CI (UI-07)</name>
  <read_first>
    - src/components/layout/Navbar.tsx (`slot="end"` flex group: ThemeToggle, LanguageToggle, HamburgerButton, `gap-component-sm`)
    - src/components/layout/ThemeToggle.test.tsx and LanguageToggle.test.tsx (existing assertions referencing the old markup)
  </read_first>
  <action>
    Adjust the `slot="end"` gap so the smaller controls sit comfortably (tighten if needed).
    Update `ThemeToggle.test.tsx` to assert the switch markup + that activation calls the
    theme hook, and `LanguageToggle.test.tsx` to assert the pill markup + that activating the
    inactive segment triggers locale navigation. Then run the full CI gate.
  </action>
  <verify>
    <automated>npm run lint && npx tsc --noEmit && npm run test && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Navbar `slot="end"` renders both new controls + hamburger without layout breakage
    - ThemeToggle/LanguageToggle tests updated and passing
    - lint, typecheck, test, build all exit 0; coverage thresholds hold
  </acceptance_criteria>
  <done>Navbar spacing tidy; tests updated; full CI green.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03-01 | N/A | Theme/locale toggle UI | accept | Same persistence (localStorage theme) and same `router.replace` locale routing; only the control's markup/affordance changes. No new surface. |

No high-severity threats.
</threat_model>

<verification>
- Theme switch flips light/dark and persists across reload
- Language pill highlights the active locale and navigates on switch; keyboard-accessible
- `npm run lint && npx tsc --noEmit && npm run test && npm run build` exit 0
</verification>

<success_criteria>
- UI-07: Theme = sun/moon sliding `sgds-switch`; language = segmented EN|ID pill; both compact, in the navbar; existing theme/locale behavior and a11y preserved.
</success_criteria>

<output>
Create `.planning/phases/06-look-feel-polish/06-03-SUMMARY.md` when done.
</output>
