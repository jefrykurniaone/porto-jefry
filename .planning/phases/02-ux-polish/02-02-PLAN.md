---
phase: 02-ux-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/[locale]/not-found.tsx
  - src/i18n/messages/en.json
  - src/i18n/messages/id.json
autonomous: true
requirements: [UX-02]

must_haves:
  truths:
    - Navigating to a non-existent URL (e.g., /en/invalid) shows a branded 404 page instead of Next.js default
    - The 404 page displays in the correct locale (EN or ID) based on URL
    - The 404 page includes a "Return Home" button that navigates back to the home page
    - The page uses the same fonts, colors, and spacing as the main portfolio site
  artifacts:
    - path: "src/app/[locale]/not-found.tsx"
      provides: "Custom locale-aware 404 page component"
      min_lines: 35
      contains: "useTranslations, useLocale, Link, FileQuestion icon"
    - path: "src/i18n/messages/en.json"
      provides: "English 404 page translations"
      contains: "notFound.title, notFound.message, notFound.returnHome"
    - path: "src/i18n/messages/id.json"
      provides: "Indonesian 404 page translations"
      contains: "notFound.title, notFound.message, notFound.returnHome"
  key_links:
    - from: "not-found.tsx"
      to: "next-intl hooks"
      via: "useTranslations('notFound'), useLocale()"
      pattern: "useTranslations.*notFound.*useLocale"
    - from: "Return Home button"
      to: "locale home page"
      via: "Link from @/i18n/routing"
      pattern: "Link.*href=.*locale"
    - from: "not-found.tsx"
      to: "notFound translation keys"
      via: "t function calls"
      pattern: "t\\('title'\\).*t\\('message'\\).*t\\('returnHome'\\)"
---

<objective>
Create a custom locale-aware 404 page that matches the portfolio's branding and provides clear navigation back to the home page.

Purpose: Replace the generic Next.js 404 page with a branded experience (D-05, D-06) — users see consistent design when navigating to non-existent URLs, reducing confusion and improving perceived quality.

Output: `src/app/[locale]/not-found.tsx` component rendering a centered 404 message with FileQuestion icon, localized title/message, and "Return Home" CTA.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-ux-polish/02-CONTEXT.md
@.planning/phases/02-ux-polish/02-UI-SPEC.md
@src/app/[locale]/layout.tsx
@src/components/sections/Hero.tsx
@src/i18n/messages/en.json
@src/i18n/messages/id.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add 404 translation keys to both locales</name>
  <files>
    src/i18n/messages/en.json
    src/i18n/messages/id.json
  </files>
  <behavior>
    - Test 1: notFound namespace exists in both en.json and id.json
    - Test 2: All three keys (title, message, returnHome) are present
    - Test 3: Strings are non-empty and match copywriting contract from UI-SPEC.md
  </behavior>
  <action>
Add `notFound` namespace with translation keys to both locale files (per D-06 UI spec).

**In en.json (add after "hero" namespace, before "about"):**
```json
"notFound": {
  "title": "Page Not Found",
  "message": "The page you're looking for doesn't exist or has been moved.",
  "returnHome": "Return Home"
},
```

**In id.json (add after "hero" namespace, before "about"):**
```json
"notFound": {
  "title": "Halaman Tidak Ditemukan",
  "message": "Halaman yang Anda cari tidak ada atau telah dipindahkan.",
  "returnHome": "Kembali ke Beranda"
},
```

**Validation:**
- Keys match UI-SPEC.md copywriting contract exactly
- Indonesian translations are natural and idiomatic
- JSON syntax is valid (no trailing commas, proper nesting)
  </action>
  <verify>
    <automated>node -e "const en = require('./src/i18n/messages/en.json'); const id = require('./src/i18n/messages/id.json'); console.assert(en.notFound && id.notFound, 'notFound namespace missing'); console.log('✅ Translation keys valid');"</automated>
  </verify>
  <done>
- `notFound` namespace exists in both en.json and id.json
- All three keys (title, message, returnHome) are present in both locales
- Strings match UI-SPEC.md copywriting contract
- JSON files parse without errors
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create custom not-found.tsx component</name>
  <files>
    src/app/[locale]/not-found.tsx
  </files>
  <behavior>
    - Test 1: Component renders without errors
    - Test 2: FileQuestion icon from lucide-react is present
    - Test 3: Title heading (h1) displays translated notFound.title
    - Test 4: Message paragraph displays translated notFound.message
    - Test 5: Return Home button displays translated notFound.returnHome
    - Test 6: Button links to /{locale} using Link from @/i18n/routing
    - Test 7: Component uses useLocale to get current locale
  </behavior>
  <action>
Create custom 404 page component at `src/app/[locale]/not-found.tsx` (per D-05, D-06).

**File structure per UI-SPEC.md layout:**

```tsx
'use client';

import { useTranslations, useLocale } from 'next-intl';
import { Link } from '@/i18n/routing';
import { FileQuestion } from 'lucide-react';

export default function NotFound() {
  const t = useTranslations('notFound');
  const locale = useLocale();

  return (
    <div className="min-h-screen flex items-center justify-center px-4">
      <div className="text-center max-w-md">
        <FileQuestion 
          size={64} 
          className="mx-auto mb-6 text-gray-400 dark:text-gray-500" 
          aria-hidden="true"
        />
        <h1 className="text-4xl font-semibold mb-4 text-gray-900 dark:text-white">
          {t('title')}
        </h1>
        <p className="text-lg text-gray-600 dark:text-gray-300 mb-8">
          {t('message')}
        </p>
        <Link href={`/${locale}`}>
          <button 
            type="button"
            className="px-6 py-4 bg-blue-600 hover:bg-blue-700 text-white font-semibold rounded-xl transition-colors"
          >
            {t('returnHome')}
          </button>
        </Link>
      </div>
    </div>
  );
}
```

**Implementation notes:**
- Use `'use client'` directive (required for useTranslations and useLocale hooks)
- Import Link from `@/i18n/routing` (not next/navigation) for locale-aware navigation
- FileQuestion icon size 64px per UI spec
- Icon has `aria-hidden="true"` (decorative, not semantic)
- Spacing matches UI spec: mb-6 (icon-title), mb-4 (title-message), mb-8 (message-button)
- Button styling matches Hero CTA button (primary blue theme)
- Button has `type="button"` attribute (per project conventions)
- Typography: text-4xl (36px) for title, text-lg (18px) for message
- Dark mode support via `dark:` Tailwind variants

**File location:** `src/app/[locale]/not-found.tsx` (must be in locale directory per Next.js App Router conventions)
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint</automated>
  </verify>
  <done>
- `src/app/[locale]/not-found.tsx` file exists
- Component uses `'use client'` directive
- Imports useTranslations, useLocale, Link from correct sources
- Renders FileQuestion icon, h1 title, p message, and button
- Button links to `/{locale}` using locale-aware Link
- Styling matches UI spec (spacing, typography, colors, dark mode)
- TypeScript and lint checks pass
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify 404 page behavior in both locales</name>
  <files>
    src/app/[locale]/not-found.tsx
  </files>
  <action>
Manually verify the custom 404 page renders correctly in both EN and ID locales.

**Verification steps:**

1. Start dev server: `npm run dev`

2. Test English locale:
   - Navigate to `http://localhost:3000/en/this-does-not-exist`
   - Verify branded 404 page appears (not Next.js default)
   - Verify text in English:
     - Title: "Page Not Found"
     - Message: "The page you're looking for doesn't exist or has been moved."
     - Button: "Return Home"
   - Click "Return Home" button
   - Verify redirects to `/en` (English home page)

3. Test Indonesian locale:
   - Navigate to `http://localhost:3000/id/halaman-tidak-ada`
   - Verify branded 404 page appears
   - Verify text in Indonesian:
     - Title: "Halaman Tidak Ditemukan"
     - Message: "Halaman yang Anda cari tidak ada atau telah dipindahkan."
     - Button: "Kembali ke Beranda"
   - Click button
   - Verify redirects to `/id` (Indonesian home page)

4. Visual consistency check:
   - Verify FileQuestion icon is gray (not blue or black)
   - Verify title is large and bold (36px, semibold)
   - Verify button matches Hero CTA styling (blue background, white text, rounded-xl)
   - Verify dark mode works (switch theme toggle, verify colors invert correctly)
   - Verify spacing matches UI spec (icon → title 24px, title → message 16px, message → button 32px)

5. Accessibility check:
   - Tab to button (verify keyboard focus visible)
   - Press Enter on button (verify navigation works)
   - Use screen reader (verify h1 and p are announced, icon is not announced)

**Document findings:**
- Screenshot both EN and ID versions (optional but helpful)
- Note any deviations from UI spec
- Confirm navigation works correctly
  </action>
  <verify>
    <automated>npm run lint && npx tsc --noEmit</automated>
  </verify>
  <done>
- 404 page renders in both `/en/*` and `/id/*` routes
- English translations display correctly
- Indonesian translations display correctly
- "Return Home" button navigates to correct locale home page
- Visual styling matches UI spec and existing site design
- Dark mode support works correctly
- Keyboard navigation and screen reader accessibility confirmed
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User → 404 Page | User navigates to non-existent URL |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Spoofing | Link navigation | accept | Link component from @/i18n/routing is already validated by next-intl; locale param is constrained to ['en', 'id'] by routing config |
| T-02-05 | Information Disclosure | 404 page content | accept | Generic "Page Not Found" message; no sensitive path information or internal structure exposed |
| T-02-06 | Denial of Service | 404 rendering | accept | Static page render with no API calls or database queries; no DoS vector |

</threat_model>

<verification>
## Acceptance Criteria

Run verification suite in order:

```bash
# 1. Lint
npm run lint

# 2. Type check
npx tsc --noEmit

# 3. Build (confirms 404 page compiles)
npm run build
```

All commands must pass without warnings or errors.

## Manual Verification

1. Start dev server: `npm run dev`
2. Navigate to non-existent URLs:
   - `/en/invalid-page`
   - `/id/halaman-tidak-ada`
3. Verify custom 404 page appears (not Next.js default)
4. Verify translations match locale
5. Click "Return Home" button in both locales
6. Verify navigation to correct home page
7. Toggle dark mode and verify styling
</verification>

<success_criteria>
## Must Be True

✅ Navigating to a non-existent URL shows the branded not-found.tsx page in the correct locale instead of the Next.js default

## Measurable Outcomes

1. **Component exists**: `src/app/[locale]/not-found.tsx` file created
2. **Translation coverage**: `notFound` namespace with 3 keys in both en.json and id.json
3. **Locale awareness**: Component uses `useLocale` to determine current locale
4. **Navigation**: Link uses `@/i18n/routing` and targets `/{locale}`
5. **Visual branding**: FileQuestion icon, semantic HTML (h1, p), branded button styling
6. **Accessibility**: Icon has `aria-hidden`, button is keyboard navigable
7. **Dark mode**: All text and background colors have `dark:` variants
8. **Type safe**: `npx tsc --noEmit` passes
9. **Lint clean**: `npm run lint` passes with zero warnings
10. **Build success**: `npm run build` completes without errors
</success_criteria>

<output>
Create `.planning/phases/02-ux-polish/02-02-SUMMARY.md` when done.

Summary must include:
- Requirements completed: UX-02
- Files created count and list
- Manual verification results (EN and ID locales)
- Screenshots or visual confirmation (optional)
- Deviations from plan (if any)
</output>
