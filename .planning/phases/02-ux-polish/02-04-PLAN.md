---
phase: 02-ux-polish
plan: 04
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: [UX-04]

must_haves:
  truths:
    - ThemeToggle renders a server-side stable placeholder icon to prevent layout shift during hydration
    - The placeholder reserves the exact space needed for the icon (w-9 h-9)
    - The placeholder is hidden once the client component hydrates and renders the real icon
    - CLS (Cumulative Layout Shift) for the ThemeToggle is 0 (no icon pop-in or shift)
  artifacts:
    - path: "src/components/layout/ThemeToggle.tsx"
      provides: "Server-side placeholder that prevents CLS"
      contains: "data-testid=\"theme-toggle-placeholder\", aria-hidden=\"true\", w-9 h-9"
    - path: "src/components/layout/ThemeToggle.client.tsx"
      provides: "Client-side theme toggle with actual icon"
      contains: "useTheme, Sun, Moon icons"
  key_links:
    - from: "ThemeToggle.tsx (server)"
      to: "ThemeToggle.client.tsx"
      via: "imports and renders client component"
      pattern: "import.*ThemeToggleClient.*<ThemeToggleClient"
    - from: "Placeholder span"
      to: "CSS dimensions"
      via: "className w-9 h-9"
      pattern: "w-9 h-9"
---

<objective>
Verify that the ThemeToggle CLS fix from Phase 1 is working correctly — no new implementation needed.

Purpose: Confirm that decision D-08 is satisfied — ThemeToggle renders its icon immediately on hydration with no layout shift (CLS = 0).

Output: Verification report confirming the server-side placeholder pattern prevents icon pop-in, no code changes required.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-ux-polish/02-CONTEXT.md
@.planning/phases/01-quick-bug-fixes/01-SUMMARY.md
@src/components/layout/ThemeToggle.tsx
@src/components/layout/ThemeToggle.client.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify server-side placeholder exists</name>
  <files>
    src/components/layout/ThemeToggle.tsx
  </files>
  <action>
Inspect `src/components/layout/ThemeToggle.tsx` to confirm the server-side placeholder pattern is implemented correctly.

**Expected implementation (per Phase 1 SUMMARY):**
- File exists at `src/components/layout/ThemeToggle.tsx`
- Server component (no `'use client'` directive)
- Renders a placeholder `<span>` with exact dimensions (w-9 h-9) to reserve space
- Placeholder has `aria-hidden="true"` (decorative, not semantic)
- Placeholder contains a simple SVG circle (stable fallback icon)
- Component imports and renders `ThemeToggleClient` (the actual client component)

**Verification steps:**
1. Read `src/components/layout/ThemeToggle.tsx` lines 1-27
2. Confirm structure:
   - Line 1: `import ThemeToggleClient from './ThemeToggle.client';`
   - Lines 7-22: Placeholder span with `w-9 h-9` and SVG
   - Line 23: `<ThemeToggleClient />` render
3. Confirm placeholder attributes:
   - `data-testid="theme-toggle-placeholder"` (for testing)
   - `aria-hidden="true"` (not announced to screen readers)
   - `className="w-9 h-9 inline-block"` (exact dimensions)
4. Confirm SVG dimensions match (20x20 icon in 36x36 container)

**No changes needed** — this was already implemented in Phase 1. Task is verification only.
  </action>
  <verify>
    <automated>grep -c 'data-testid="theme-toggle-placeholder"' src/components/layout/ThemeToggle.tsx | grep -v '^#' | grep 1</automated>
  </verify>
  <done>
- `src/components/layout/ThemeToggle.tsx` contains server-side placeholder
- Placeholder has `w-9 h-9` dimensions
- Placeholder has `aria-hidden="true"` attribute
- Placeholder renders before `ThemeToggleClient`
- File structure matches Phase 1 implementation
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify client component hydration</name>
  <files>
    src/components/layout/ThemeToggle.client.tsx
  </files>
  <action>
Inspect `src/components/layout/ThemeToggle.client.tsx` to confirm the client-side icon rendering is implemented correctly.

**Expected implementation (per Phase 1 SUMMARY):**
- File exists at `src/components/layout/ThemeToggle.client.tsx`
- Client component (has `'use client'` directive)
- Uses `useTheme` hook from `next-themes`
- Renders Sun icon (light mode) or Moon icon (dark mode)
- Button has `type="button"` attribute (per FIX-01)
- Button has aria-label for accessibility

**Verification steps:**
1. Read `src/components/layout/ThemeToggle.client.tsx`
2. Confirm structure:
   - Line 1: `'use client';`
   - Imports: `useTheme` from `next-themes`, `Sun`, `Moon` from `lucide-react`
   - Button type: `type="button"`
   - Icon conditional: `resolvedTheme === 'dark' ? <Sun> : <Moon>`
3. Confirm button attributes:
   - `type="button"` (prevents form submission)
   - `aria-label` (localized toggle description)
   - `onClick` calls `setTheme` with opposite theme

**No changes needed** — this was already implemented in Phase 1. Task is verification only.
  </action>
  <verify>
    <automated>grep -c 'use client' src/components/layout/ThemeToggle.client.tsx | grep 1</automated>
  </verify>
  <done>
- `src/components/layout/ThemeToggle.client.tsx` is a client component
- Component uses `useTheme` hook
- Renders Sun/Moon icons conditionally based on theme
- Button has `type="button"` attribute
- File structure matches Phase 1 implementation
  </done>
</task>

<task type="auto">
  <name>Task 3: Run existing tests to confirm CLS prevention</name>
  <files>
    src/components/layout/ThemeToggle.test.tsx
  </files>
  <action>
Run existing tests for ThemeToggle to confirm the server-side placeholder pattern is working.

**Expected test coverage (per Phase 1 SUMMARY):**
- Test file exists: `src/components/layout/ThemeToggle.test.tsx`
- Tests verify placeholder renders (server-side)
- Tests verify client component renders after hydration
- Tests verify button type and aria-label

**Verification steps:**
1. Run tests: `npm test -- ThemeToggle`
2. Confirm all tests pass
3. Review test output for:
   - "renders server-side placeholder" test
   - "button has type='button'" test
   - "renders theme toggle button" test
4. Check coverage report: `npm run test:coverage -- --reporter=text --run src/components/layout/ThemeToggle.test.tsx`
5. Confirm coverage above 80% threshold

**Expected result:** All tests pass, confirming the placeholder pattern is working correctly.
  </action>
  <verify>
    <automated>npm test -- ThemeToggle</automated>
  </verify>
  <done>
- ThemeToggle.test.tsx runs without errors
- All test cases pass (server placeholder, client button, aria attributes)
- Test coverage above 80% threshold
- No regressions detected
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
ThemeToggle CLS fix was implemented in Phase 1. The server-side placeholder pattern reserves space before client hydration, preventing icon pop-in and layout shift.

**Implementation details:**
- `ThemeToggle.tsx`: Server component with placeholder span (w-9 h-9) and simple SVG circle
- `ThemeToggle.client.tsx`: Client component with actual Sun/Moon icons from lucide-react
- Placeholder hidden via CSS once client component hydrates and renders

**Expected behavior:**
- On initial page load, placeholder SVG circle appears immediately (no blank space)
- Once JavaScript loads, placeholder is replaced by actual theme icon
- No layout shift occurs during this transition (CLS = 0)
  </what-built>
  <how-to-verify>
**Visual verification (manual):**

1. Start dev server: `npm run dev`

2. Test initial page load with network throttling:
   - Open DevTools → Network tab
   - Set throttling to "Slow 3G" or "Fast 3G"
   - Navigate to `http://localhost:3000/en`
   - Watch the Navbar during page load
   - **Confirm:** ThemeToggle icon appears immediately (no blank space or pop-in)

3. Test without JavaScript (server render only):
   - Open DevTools → Network tab
   - Disable JavaScript (DevTools → Settings → Debugger → Disable JavaScript)
   - Hard refresh the page (Ctrl+Shift+R or Cmd+Shift+R)
   - **Confirm:** Placeholder SVG circle is visible in the navbar
   - Re-enable JavaScript

4. Test theme toggle functionality:
   - Click the ThemeToggle button
   - **Confirm:** Theme switches (light ↔ dark)
   - **Confirm:** Icon changes (Sun ↔ Moon)
   - **Confirm:** No layout shift when icon changes

5. Test with React DevTools:
   - Install React DevTools browser extension
   - Open React DevTools → Components tab
   - Find ThemeToggle component in tree
   - **Confirm:** Server component renders placeholder, then client component hydrates

6. Measure CLS with Lighthouse:
   - Open DevTools → Lighthouse tab
   - Run audit for Performance (Desktop or Mobile)
   - Review "Cumulative Layout Shift" metric
   - **Confirm:** CLS for ThemeToggle is 0 (no shift detected)

**Expected outcome:**
- Placeholder appears immediately on page load
- No blank space or icon pop-in during hydration
- CLS = 0 for ThemeToggle area
- Theme toggle works correctly after hydration

**Screenshots (optional but helpful):**
- Screenshot 1: Initial page load with placeholder visible
- Screenshot 2: After hydration with actual icon rendered
- Screenshot 3: Lighthouse CLS metric showing 0
  </how-to-verify>
  <resume-signal>
Type "approved" if ThemeToggle icon appears immediately with no layout shift (CLS = 0), or describe any issues observed.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | No new trust boundaries — verification only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-10 | N/A | ThemeToggle | N/A | No new threats — existing implementation verified only |

</threat_model>

<verification>
## Acceptance Criteria

Run verification suite in order:

```bash
# 1. Lint
npm run lint

# 2. Type check
npx tsc --noEmit

# 3. Tests
npm test -- ThemeToggle

# 4. Coverage
npm run test:coverage -- --reporter=text --run src/components/layout/ThemeToggle.test.tsx
```

All commands must pass without warnings or errors.

## Manual Verification (Human Checkpoint)

See Task 4 checkpoint for detailed verification steps. Human must confirm:
- Placeholder appears immediately on page load
- No icon pop-in or layout shift during hydration
- Theme toggle works correctly after hydration
- Lighthouse CLS = 0 for ThemeToggle

## Visual Verification Tools

- **Network throttling**: Slow 3G to observe hydration delay
- **Disable JavaScript**: Confirm server-side placeholder renders
- **React DevTools**: Confirm server → client hydration flow
- **Lighthouse**: Measure CLS metric for ThemeToggle area
</verification>

<success_criteria>
## Must Be True

✅ ThemeToggle renders its icon immediately on hydration — no icon pop-in or layout shift (CLS = 0 for the toggle)

## Measurable Outcomes

1. **Server placeholder exists**: `src/components/layout/ThemeToggle.tsx` has placeholder span with w-9 h-9 dimensions
2. **Client component exists**: `src/components/layout/ThemeToggle.client.tsx` renders actual Sun/Moon icons
3. **Test coverage**: `ThemeToggle.test.tsx` includes tests for placeholder and client button
4. **Tests pass**: `npm test -- ThemeToggle` passes all test cases
5. **Coverage threshold**: ThemeToggle coverage above 80%
6. **Visual confirmation**: Human verifies no icon pop-in during page load
7. **CLS = 0**: Lighthouse audit confirms no layout shift for ThemeToggle
8. **Functionality works**: Theme toggle button switches theme correctly after hydration
9. **Lint clean**: `npm run lint` passes with zero warnings
10. **Type safe**: `npx tsc --noEmit` passes

## Verification Method

This plan uses **verification-first execution** pattern (per Phase 1 learning):
1. No new code to implement (fix already exists from Phase 1)
2. Inspect existing implementation to confirm pattern
3. Run existing tests to confirm behavior
4. Human checkpoint verifies visual CLS prevention
5. Document findings in SUMMARY
</success_criteria>

<output>
Create `.planning/phases/02-ux-polish/02-04-SUMMARY.md` when done.

Summary must include:
- Requirements completed: UX-04
- Confirmation that Phase 1 implementation is working correctly
- Test results (all passing)
- Manual verification results (human checkpoint findings)
- Lighthouse CLS metric (0 = success)
- No code changes needed (verification only)
</output>
