---
phase: 3
phase_name: "Security Hardening"
project: "porto-jefry"
generated: "2026-06-03T18:06:33+08:00"
counts:
  decisions: 3
  lessons: 4
  patterns: 2
  surprises: 3
missing_artifacts:
  - "03-VERIFICATION.md"
  - "03-UAT.md"
---

# Phase 3 Learnings: Security Hardening

> Note: These learnings are extracted from the **03-01 ad-hoc CSP hotfix** (pre-dates formal Phase 3 execution). Full Phase 3 learnings will be appended after SEC-01 and the remainder of SEC-02 are completed.

---

## Decisions

### Use `unsafe-inline` for `style-src` Rather Than a Static Hash
The production CSP `style-src` was updated to include `'unsafe-inline'` to unblock the `@font-face` inline `<style>` generated by `next/font/google`. The alternative — embedding the static SHA-256 hash of the style content — was evaluated and rejected because the hash changes whenever the font configuration changes (subsets, weights, display mode), making it brittle. CSS injection cannot execute code, so `unsafe-inline` for styles poses far lower XSS risk than for scripts.

**Rationale:** Pragmatic security trade-off — CSS is not an XSS vector the way scripts are; the strict nonce-based `script-src` with `'strict-dynamic'` is the critical protection boundary.  
**Source:** 03-01-PLAN.md

---

### Treat Browser Extension Console Errors as Non-Actionable
When triaging console errors in production, the "A listener indicated an asynchronous response by returning true, but the message channel closed before a response was received" error was explicitly classified as a browser extension artifact and not a site defect. No fix was pursued.

**Rationale:** This error originates from extensions' `chrome.runtime.onMessage` listeners returning `true` (indicating async intent) without responding — the website cannot control or suppress it.  
**Source:** 03-01-PLAN.md

---

### Pass Per-Request Nonce from Middleware to ThemeProvider Explicitly
Although `next-themes` v0.4.x supports a `nonce` prop on `ThemeProvider`, it is not automatically wired to the nonce generated in middleware. The layout must explicitly read `x-nonce` from request headers and pass it as a prop.

**Rationale:** Next.js automatic nonce propagation applies to Next.js-internal scripts (`__NEXT_DATA__`, `next/script`) — it does not reach third-party library inline scripts. Explicit wiring is required for any library that injects inline scripts.  
**Source:** 03-01-PLAN.md, 03-01-SUMMARY.md

---

## Lessons

### `next/font/google` Inline Styles Do Not Receive the Middleware Nonce in Next.js 14.2
Even though the CSP nonce is generated per-request in middleware and set via the `x-nonce` request header (the documented Next.js pattern), the `@font-face` inline `<style>` tag produced by `Inter()` from `next/font/google` does not receive the nonce. The font function is called at module level, outside the per-request render context, preventing dynamic nonce injection.

**Context:** Next.js documentation states that `x-nonce` is used for auto-nonce on internally generated scripts, but this does not extend to `next/font` inline style tags in Next.js 14.2.35.  
**Source:** 03-01-SUMMARY.md

---

### Third-Party Library Inline Scripts Require Explicit Nonce Wiring
Adding a strict `script-src` CSP with per-request nonces requires proactively auditing every third-party library used in the app for inline script injection. Libraries that inject inline scripts (like `next-themes` for theme detection) need the nonce passed explicitly — they do not benefit from Next.js's automatic nonce propagation.

**Context:** The `next-themes` ThemeProvider injected a blocking inline script that was silently blocked by the CSP. The theme still appeared to work (fallback CSS), masking the error until DevTools was opened.  
**Source:** 03-01-SUMMARY.md

---

### Silent CSP Violations Can Mask Real Functionality Issues
The CSP violations were not causing visible UI breakage — the page loaded correctly, the theme appeared to switch, and fonts rendered. The errors were only visible in the browser console. This means production CSP errors can silently degrade security intent (scripts being blocked that shouldn't be, or conversely, security not applied as intended) without obvious user-facing symptoms.

**Context:** The ThemeProvider script being blocked meant the theme class was not being applied before React hydration — theme flash prevention was silently broken.  
**Source:** 03-01-SUMMARY.md

---

### Phase 3 Hotfix Created Partial Overlap with Planned SEC-02 Work
The ad-hoc fix added `'unsafe-inline'` to `style-src` but left `fonts.googleapis.com` in the CSP. The formal Phase 3 SEC-02 plan was to remove `fonts.googleapis.com` and `fonts.gstatic.com` entirely (since `next/font/google` self-hosts fonts at build time). The hotfix and the planned work now need to be reconciled: the formal execution should still remove the Google Fonts domains (now harmless since `unsafe-inline` covers the inline style).

**Context:** Ad-hoc hotfixes that touch the same files as planned phase work can drift from the phase's architectural intent. The CONTEXT.md decision log (D-04, D-05) must be re-evaluated before Phase 3 formal execution.  
**Source:** 03-01-SUMMARY.md, 03-CONTEXT.md

---

## Patterns

### Read `x-nonce` in Server Component Layout and Distribute Downward
When using a strict CSP with per-request nonces, the server layout component is the single place where `headers()` is called to read `x-nonce`. The nonce is then distributed as a prop to any child component that injects inline scripts (e.g., ThemeProvider, analytics). This keeps nonce management centralized in one component rather than scattered across the tree.

```tsx
// src/app/[locale]/layout.tsx
import { headers } from 'next/headers';
// ...
const nonce = (await headers()).get('x-nonce') ?? undefined;
// ...
<ThemeProvider nonce={nonce}>
```

**When to use:** Any time a third-party component needs to inject an inline script and the app uses a strict nonce-based `script-src` CSP.  
**Source:** 03-01-PLAN.md, 03-01-SUMMARY.md

---

### Triage Console Errors by Source Before Acting
When triaging a batch of browser console errors, classify each by source before fixing:
1. **Our code** (module path points to `src/`) → fix
2. **Third-party library** (module path points to `node_modules/`) → evaluate if we can fix via config
3. **Browser extension** (no module path, or generic promise rejection) → document as non-actionable

This avoids wasting effort on errors outside the site's control.

**When to use:** During production monitoring, post-deploy review, or any session where DevTools console errors are being triaged.  
**Source:** 03-01-PLAN.md

---

## Surprises

### `next-themes` Nonce Support Was Present But Never Connected
`next-themes` v0.4.x added a `nonce` prop to `ThemeProvider`, but the layout had never wired it up to the middleware-generated nonce. The CSP infrastructure (nonce generation in middleware, `x-nonce` header forwarding) was fully in place — the missing link was just the prop. The feature existed in both the library and the infrastructure; only the connection was absent.

**Impact:** The theme-detection inline script was blocked by the strict CSP on every page load in production, silently breaking the "no flash of wrong theme" feature. Since the CSS fallback still applied a theme class, the breakage was invisible to casual observation.  
**Source:** 03-01-SUMMARY.md

---

### Next.js 14 Does Not Auto-Nonce `next/font` Inline Styles
The assumption that setting `x-nonce` in middleware request headers would cause Next.js to automatically apply it to all inline elements (including `next/font` style tags) was incorrect. Next.js auto-nonces only its own internally generated `<script>` tags. Font inline styles require a different mitigation (`unsafe-inline` or static hash).

**Impact:** The CSP `style-src` had to be relaxed to `unsafe-inline` for production, which is a weaker policy than originally intended. The formal Phase 3 SEC-02 work should still remove unnecessary `fonts.googleapis.com` and `fonts.gstatic.com` entries to partially compensate.  
**Source:** 03-01-SUMMARY.md

---

### "Message Channel Closed" Error Is a Browser Extension Artifact
The third console error — "A listener indicated an asynchronous response by returning true, but the message channel closed before a response was received" — appeared alarming (unhandled promise rejection) but was not caused by the website. It is generated by browser extensions (typically password managers or ad blockers) that register `chrome.runtime.onMessage` listeners and return `true` without completing the async response.

**Impact:** Zero impact on the site. Time spent confirming this was not our code: ~2 minutes. It's worth recognizing this pattern early to avoid chasing it as a real bug.  
**Source:** 03-01-PLAN.md
